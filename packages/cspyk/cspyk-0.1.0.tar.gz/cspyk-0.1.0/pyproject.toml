[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cspyk"
version = "0.1.0"
description = "Jupyter kernel provisioner package for Cybershuttle"
authors = [ { name = "Dimuthu Wannipurage" } ]
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["jupyter-client>=7.0"]
classifiers = [
  "Topic :: Utilities",
  "Framework :: Jupyter",
]
license = { text = "Apache-2" }

[project.scripts]
cspyk-kernel = "cspyk.kernelapp:main"

# Integrate with Jupyter Client
[project.entry-points."jupyter_client.kernel_provisioners"]
cspyk-provisioner = "cspyk.provisioning:CSKernelProvisioner"

[tool.setuptools.packages.find]
where = ["."]
include = ["cspyk*"]
exclude = ["venv*", ".venv*", "env*", "build", "dist", "tests"]