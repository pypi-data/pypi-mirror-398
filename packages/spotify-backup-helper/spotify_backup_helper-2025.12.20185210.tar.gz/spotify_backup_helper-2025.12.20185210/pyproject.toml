[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spotify-backup-helper"
version = "2025.12.20185210"
description = "A new package that helps Spotify users securely back up their music library. By accepting simple text inputs that describe a user’s playlists, liked tracks, and saved albums, the tool generates a clea"
readme = "README.md"
authors = [{ name = "spotify-backup-helper", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/spotify-backup-helper"
