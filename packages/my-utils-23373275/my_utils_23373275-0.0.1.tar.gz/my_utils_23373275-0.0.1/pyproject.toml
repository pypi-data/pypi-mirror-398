[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "my_utils_23373275"
version = "0.0.1"
description = "A base package for homework"
dependencies = [
    "requests"  # 这里满足了作业要求1：依赖第三方包
]

[tool.hatch.build.targets.wheel]
packages = ["src/my_utils_23373275"]