[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "memeval"
version = "0.0.1"
description = "Memory Evaluation."
readme = "README.md"
requires-python = ">=3.8"
