[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "debugscribe"
version = "2025.12.21165719"
description = "A new package designed to assist users in understanding and resolving errors in program execution. Users can input error messages or descriptions of unexpected program behavior, and the package will u"
readme = "README.md"
authors = [{ name = "debugscribe", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/debugscribe"
