[project]
name = "test-mcp-echo-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["mcp"]

[project.scripts]
test-mcp-echo-server = "server:main"

[tool.uv]
package = true

[tool.setuptools]
py-modules = ["server"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]
