[project]
name = "botrun-hatch"
version = "5.12.261"
description = ""
authors = [{name = "sebastian-hsu", email = "sebastian.hsu@gmail.com"}]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic-settings>=2.5.2",
]

[project.optional-dependencies]
dev = [
    "build>=1.2.2",
    "twine>=5.1.1",
]

[tool.uv]
dev-dependencies = [
    "build>=1.3.0",
    "twine>=6.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
