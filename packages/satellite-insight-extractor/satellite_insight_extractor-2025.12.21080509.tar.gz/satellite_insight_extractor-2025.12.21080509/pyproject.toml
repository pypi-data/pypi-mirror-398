[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "satellite-insight-extractor"
version = "2025.12.21080509"
description = "A new package designed to streamline the extraction of structured insights from brief, domain-specific reports. It receives a short text input describing recent events—such as satellite incidents—and "
readme = "README.md"
authors = [{ name = "satellite-insight-extractor", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/satellite-insight-extractor"
