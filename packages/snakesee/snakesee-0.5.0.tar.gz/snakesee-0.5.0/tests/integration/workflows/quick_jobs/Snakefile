# Quick jobs that complete very fast
# Tests timing edge cases where jobs finish quickly

rule all:
    input:
        expand("output/job_{n}.txt", n=range(1, 11))

rule quick_job:
    output:
        "output/job_{n}.txt"
    shell:
        """
        echo "Quick job {wildcards.n}" > {output}
        """
