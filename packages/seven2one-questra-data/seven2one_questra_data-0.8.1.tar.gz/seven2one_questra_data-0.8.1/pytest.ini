[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: Unit tests (fast, mocked)
    integration: Integration tests (need real API)
    slow: Slow tests

addopts =
    -v
    --strict-markers
    -m "not integration"
    --tb=short
