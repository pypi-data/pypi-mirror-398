[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "scl-core"
version = "0.0.1"
description = "Placeholder for scl-core package"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
    {name = "Your Name", email = "your.email@example.com"},
]

[tool.hatch.build.targets.wheel]
packages = ["src/scl"]
