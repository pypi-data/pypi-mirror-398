[project]
name = "fastapi-test-pkg"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nipu", email = "myemail@yahoo.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fastapi_test_pkg"]
include = ["src/fastapi_test_pkg/py.typed"]
