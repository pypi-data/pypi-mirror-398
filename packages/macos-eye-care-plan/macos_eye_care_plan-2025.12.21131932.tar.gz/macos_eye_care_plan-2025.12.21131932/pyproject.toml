[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "macos-eye-care-plan"
version = "2025.12.21131932"
description = "A new package designed to help users maintain healthy eye habits while using macOS devices. The package takes user input describing their daily screen usage patterns, work environment, and any existin"
readme = "README.md"
authors = [{ name = "macos-eye-care-plan", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/macos-eye-care-plan"
