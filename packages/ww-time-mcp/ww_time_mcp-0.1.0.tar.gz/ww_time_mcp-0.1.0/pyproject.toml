[project]
name = "ww-time-mcp"
version = "0.1.0"
description = "The MCP server to get local time"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.7.0",
]

[tool.uv.workspace]
members = ["venv"]

[project.scripts]
ww-time-mcp = "ww_time_mcp.server:main"

