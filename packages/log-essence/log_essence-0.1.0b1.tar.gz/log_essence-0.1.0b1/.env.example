# log-essence environment variables
# Copy this file to .env and fill in your values

# ============================================================================
# TTS Configuration (for demo generation only)
# ============================================================================
# These are only needed if you use `log-essence demo generate`

# TTS API endpoint (OpenAI-compatible)
# Examples:
#   - Kokoro local: http://localhost:8880/v1
#   - OpenAI: https://api.openai.com/v1
LOG_ESSENCE_TTS_BASE_URL=http://localhost:8880/v1

# TTS API key (required for OpenAI, not needed for Kokoro)
LOG_ESSENCE_TTS_API_KEY=

# TTS voice to use
# Kokoro female: af_sky (soothing), af_bella, af_heart, af_nova, af_sarah
# Kokoro male: am_adam, am_michael, am_eric
# OpenAI: alloy, echo, fable, onyx, nova, shimmer
LOG_ESSENCE_TTS_VOICE=af_sky

# ============================================================================
# Whisper Configuration (for audio timing extraction)
# ============================================================================
# Used to extract word-level timestamps for video synchronization.
# Falls back to ffprobe duration if Whisper is unavailable.

# Whisper API endpoint
# Examples:
#   - WhisperX local: http://localhost:8000
#   - OpenAI: https://api.openai.com/v1
LOG_ESSENCE_WHISPER_BASE_URL=http://localhost:8000

# Whisper API key (required for OpenAI, not needed for WhisperX)
LOG_ESSENCE_WHISPER_API_KEY=

# ============================================================================
# FFmpeg Configuration (for video processing and text overlays)
# ============================================================================
# Used for adding text overlays synced with audio, concatenating clips, and GIF generation.

# FFmpeg API endpoint
# Examples:
#   - Local: http://localhost:3030
LOG_ESSENCE_FFMPEG_BASE_URL=http://localhost:3030

# FFmpeg API key (if required by your deployment)
LOG_ESSENCE_FFMPEG_API_KEY=

# ============================================================================
# Notes
# ============================================================================
# The core log analysis features (CLI, MCP server, Web UI) do NOT require
# any API keys. They use:
#   - Drain3 for template extraction (local, no API)
#   - FastEmbed with BAAI/bge-small-en-v1.5 (downloads once, runs locally)
#   - tiktoken for token counting (local, no API)
#
# The TTS configuration is ONLY needed for the demo generation feature.
