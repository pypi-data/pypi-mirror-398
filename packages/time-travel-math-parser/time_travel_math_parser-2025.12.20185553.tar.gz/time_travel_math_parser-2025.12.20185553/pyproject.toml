[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "time-travel-math-parser"
version = "2025.12.20185553"
description = "The package lets users input a textual description of a claim about paradox‑free time travel backed by mathematics. It then uses an LLM together with llmatch‑messages to parse the text, extract the co"
readme = "README.md"
authors = [{ name = "time-travel-math-parser", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/time-travel-math-parser"
