[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mediainfo-py"
version = "0.1.2"
description = "Lightweight FFmpeg-based video metadata extractor"
readme = "README.md"
requires-python = ">=3.8"
license = { file = "LICENSE" }
authors = [
  { name = "Aditya Katyal" }
]

dependencies = [
  "ffmpeg-python"
]
