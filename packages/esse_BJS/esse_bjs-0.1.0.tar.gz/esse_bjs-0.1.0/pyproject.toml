[project]
name = "esse_BJS"
version = "0.1.0"
description = "NOTHING FOR NOW.."
readme = "README.md"
authors = [{ name = "BJS", email = "coderbjs@gmail.com" }]
requires-python = ">=3.13.7"
license = { text = "MIT" }

dependencies = ["colorama", "numpy"]
[tool.poetry]

package-mode = true

[tool.poetry.dependencies]

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonary.api"
