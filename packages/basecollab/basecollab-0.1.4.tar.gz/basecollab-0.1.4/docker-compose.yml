services:
  basecollab:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    environment:
      PYPI_TOKEN: ${PYPI_TOKEN}
      TEST_PYPI_TOKEN: ${TEST_PYPI_TOKEN}
    volumes:
      - ./:/app
    command: ["tail", "-f", "/dev/null"]
