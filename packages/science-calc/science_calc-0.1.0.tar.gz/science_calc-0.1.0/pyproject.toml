[project]
name = "science-calc"
version = "0.1.0"
description = "A sleek, powerful Scientific Calculator with a modern TUI."
readme = "README.md"
authors = [
    { name = "Mahir29Flame" }
]
requires-python = ">=3.8"
dependencies = [
    "textual>=0.86.0",
]

[project.scripts]
science-calc = "science_calc.main:run_app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
