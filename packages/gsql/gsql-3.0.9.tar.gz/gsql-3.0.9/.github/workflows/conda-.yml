name: 📦 Publication Anaconda

on:
  push:
    branches: main

jobs:
  conda:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      - name: 🏷️ Extraire version du tag
        id: version
        run: |
          VERSION="${GITHUB_REF#refs/tags/v}"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "📦 Version à publier: $VERSION"
      
      - name: 🐍 Setup Miniconda (Version corrigée)
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-activate-base: true
          miniconda-version: "latest"
          python-version: "3.10"
          auto-update-conda: true
      
      - name: 📦 Installer conda-build et anaconda-client
        run: |
          conda install -y conda-build anaconda-client
          conda --version
          conda-build --version
      
      - name: 🔍 Vérifier le meta.yaml existant
        run: |
          echo "🔍 Vérification du meta.yaml existant..."
          
          if [ -f "conda.recipe/meta.yaml" ]; then
            echo "✅ meta.yaml trouvé dans conda.recipe/"
            echo "📄 Premières lignes :"
            head -20 conda.recipe/meta.yaml
            
            # Vérifier que la version est mise à jour
            if grep -q "version:" conda.recipe/meta.yaml; then
              sed -i "s/version:\s*[\"'][0-9]\+\.[0-9]\+\.[0-9]\+[\"']/version: \"$VERSION\"/g" conda.recipe/meta.yaml
              echo "✅ Version mise à jour: $VERSION"
            else
              echo "⚠️ Section 'version:' non trouvée dans meta.yaml"
            fi
          else
            echo "❌ ERREUR: Fichier conda.recipe/meta.yaml introuvable !"
            ls -la conda.recipe/ 2>/dev/null || echo "Le dossier conda.recipe/ n'existe pas"
            exit 1
          fi
      
      - name: 🏗️ Construire le package Conda
        run: |
          echo "🏗️ Construction du package Conda..."
          
          # Nettoyer les anciennes builds
          rm -rf conda_pkg 2>/dev/null || true
          
          # Construire le package avec le meta.yaml existant
          conda mambabuild conda.recipe/ --output-folder conda_pkg
          
          echo "📦 Packages générés:"
          find conda_pkg -name "*.tar.bz2" 2>/dev/null | xargs ls -la || echo "Aucun package trouvé"
      
      - name: 🔍 Vérifier la structure du package
        run: |
          echo "🔍 Vérification du package généré..."
          PKG_FILE=$(find conda_pkg -name "*.tar.bz2" -type f 2>/dev/null | head -1)
          
          if [ -n "$PKG_FILE" ] && [ -f "$PKG_FILE" ]; then
            echo "✅ Package trouvé: $(basename "$PKG_FILE")"
            echo "📏 Taille: $(ls -lh "$PKG_FILE" | awk '{print $5}')"
          else
            echo "❌ Aucun package trouvé!"
            echo "📋 Contenu du dossier:"
            ls -la conda_pkg/ 2>/dev/null || echo "Dossier conda_pkg/ non trouvé"
            exit 1
          fi
      
      - name: 🚀 Upload vers Anaconda.org
        run: |
          PKG_FILE=$(find conda_pkg -name "*.tar.bz2" -type f | head -1)
          
          if [ -n "$PKG_FILE" ] && [ -f "$PKG_FILE" ]; then
            echo "📤 Upload du package vers Anaconda.org..."
            
            # Tester la connexion
            echo "🔑 Test du token Anaconda..."
            anaconda whoami || echo "⚠️ Impossible de vérifier l'utilisateur"
            
            # Uploader le package
            echo "🚀 Upload en cours..."
            anaconda -t ${{ secrets.ANACONDA_TOKEN }} upload --user ceoseshell "$PKG_FILE"
            
            echo "✅ Package uploadé avec succès!"
            echo "🌐 Disponible sur: https://anaconda.org/ceoseshell/gsql"
          else
            echo "❌ Aucun package Conda trouvé pour l'upload"
            exit 1
          fi
      
      - name: ✅ Vérification finale
        run: |
          echo "🎉 PUBLICATION ANACONDA RÉUSSIE !"
          echo "================================"
          echo "📦 Package: gsql"
          echo "🏷️ Version: $VERSION"
          echo "👤 Channel: ceoseshell"
          echo ""
          echo "🔧 Installation:"
          echo "  conda install -c ceoseshell gsql=$VERSION"
