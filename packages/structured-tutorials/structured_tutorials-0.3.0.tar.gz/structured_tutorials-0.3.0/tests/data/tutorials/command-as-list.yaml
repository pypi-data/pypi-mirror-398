configuration:
  run:
    clear_environment: true
parts:
  - commands:
      - command: [echo, "word with spaces"]
        run:
          test:
            - command: [ls, "test with spaces"]
          cleanup:
            - command: [ls, "cleanup with spaces"]