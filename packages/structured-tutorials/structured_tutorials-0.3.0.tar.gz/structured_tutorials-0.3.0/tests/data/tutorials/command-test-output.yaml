configuration:
  run:
    clear_environment: true
parts:
  - commands:
      - command: echo foo bar bla
        run:
          test:
            - regex: foo (?P<stdout>bar) bla
            - regex: foo (?P<stderr>bla) bla
              stream: stderr