[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "LzFunc"
version = "0.1.2"
authors = [
    {name = "Pozitive_Guy"},
]
description = "A small library with some 1-line functions to reduce boilerplate in the code. Also has some unusual functions. The main part contains about 5 functions, 1 class and an extra 1 error."
readme = "README.md"
license-files = ["LICENSE"]
requires-python = ">=3.8"
keywords = ["functional"]

dependencies = []
