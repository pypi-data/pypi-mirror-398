
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Rope-pos"
version = "0.1.0"
description = "Rotary Positional Embedding (RoPE) for PyTorch"
authors = [{ name = "Your Name" }]
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["torch"]


