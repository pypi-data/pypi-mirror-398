# =============================================================================
# Notify on Completion
# =============================================================================
# Send notification when a work item is marked as done.
# =============================================================================

name: Notify on done
description: Celebrate when work is completed

on: work_item.updated

when:
  state changed_to: done

do:
  - notify:
      channel: "#completions"
      message: "✅ Completed: ${{ title }}"
  
  - comment: |
      🎉 This item has been marked as done!
      
      Completed at: ${{ now }}

