experiment_name: finance_eti_v1
dataset: edgar_sample
pipeline: ETI
inference: lm-only
opik:
  api_key: ${OPIK_API_KEY}  # set as env var for real runs
  model: mock-local
  timeout: 60
seeds:
  random_seed: 42
output:
  dir: experiments/finance_eti_v1/
notes: "This is a small demo configuration that uses local mock inference when OPiK keys are not present."