[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "adsx"
version = "1.0.6"
description = "Telegram Ads Broadcast Bot"
authors = [{ name = "bleakway" }]
dependencies = [
    "telethon",
    "python-telegram-bot==20.7"
]

[project.scripts]
adsx = "adsx.main:run"
