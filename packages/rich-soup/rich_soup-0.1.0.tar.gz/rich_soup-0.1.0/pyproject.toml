[project]
name = "rich-soup"
version = "0.1.0"
description = "Parse entire web pages into JSON/Markdown."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "playwright",
    "pydantic>=2.12.5",
]
