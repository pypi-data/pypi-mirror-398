[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "schedule-llm-query"
version = "2025.12.20185642"
description = "A new package that processes user queries about event schedules, such as FOSDEM 2026, and returns structured information like session times, locations, and descriptions. It uses an LLM to interpret na"
readme = "README.md"
authors = [{ name = "schedule-llm-query", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/schedule-llm-query"
