# Kinemotion Backend Environment Configuration
# Copy this file to .env and fill in your actual values

# ========== Cloudflare R2 Configuration ==========
# Optional: If not provided, videos are processed locally and deleted after analysis

# R2 endpoint URL (e.g., https://abc123.r2.cloudflarestorage.com)
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# R2 API credentials (create in Cloudflare Dashboard)
R2_ACCESS_KEY=your_access_key_id
R2_SECRET_KEY=your_secret_access_key

# R2 bucket name (created in Cloudflare Dashboard)
R2_BUCKET_NAME=kinemotion

# ========== CORS Configuration ==========
# Optional: For production, add additional allowed origins

# Comma-separated list of additional allowed origins
# Example: CORS_ORIGINS=https://myapp.example.com,https://api.example.com
CORS_ORIGINS=

# ========== Development ==========
# Uncomment to enable debug logging
# DEBUG=1

# Server host and port (when running with uvicorn)
# HOST=0.0.0.0
# PORT=8000
