name: test_job_with_timeout_minutes
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - run: make build
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    needs:
      - build
    steps:
      - run: pytest
