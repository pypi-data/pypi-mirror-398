name: test_pipeline_mixed_dict_and_string
on:
  push:
    branches:
      - main
    paths:
      - src/**
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: pip install pygha
