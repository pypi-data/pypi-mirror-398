name: ci
on:
  push:
    branches:
      - main
jobs:
  test-matrix:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version:
          - '3.10'
          - '3.11'
      fail-fast: false
    steps: []
