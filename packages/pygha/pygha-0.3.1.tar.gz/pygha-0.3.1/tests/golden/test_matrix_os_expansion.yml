name: ci
on:
  push:
    branches:
      - main
jobs:
  test-os:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest
    steps:
      - run: echo "Running on ${{ matrix.os }}"
