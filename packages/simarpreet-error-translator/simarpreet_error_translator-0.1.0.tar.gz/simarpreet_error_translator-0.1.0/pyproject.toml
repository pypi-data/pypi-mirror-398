[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "simarpreet-error-translator"
version = "0.1.0"
description = "Explains Python errors simply."
authors = [{ name = "Simarpreet Singh" }]
dependencies = ["requests"]
