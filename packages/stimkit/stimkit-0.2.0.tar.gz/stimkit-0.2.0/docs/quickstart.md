# Quickstart

Install for local development:

```
uv run --no-progress python -m pip install -e .
```

Run an example:

```
uv run --no-progress python example/1_hello_world.py
```

Outputs are written to `example/output/`.
