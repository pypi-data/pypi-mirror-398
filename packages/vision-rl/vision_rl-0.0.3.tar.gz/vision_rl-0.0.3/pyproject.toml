[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vision_rl"
version = "0.0.3"
description = "Visual Reinforcement Learning tools"
readme = "README.md"
authors = [
    {name = "User", email = "user@example.com"},
]
dependencies = [
    "gymnasium",
    "numpy",
    "opencv-python",
    "matplotlib",
]
