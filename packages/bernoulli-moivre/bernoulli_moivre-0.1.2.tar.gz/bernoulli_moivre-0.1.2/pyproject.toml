[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bernoulli-moivre"
version = "0.1.2"
description = "Bernoulli and Moivre–Laplace probability formulas"
readme = "README.md"
requires-python = ">=3.9"

authors = [
    {name = "1032240005", email = "1032240005@pfur.ru"},
    {name = "1132240693", email = "1132240693@pfur.ru"}
]

classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent"
]

dependencies = []

[project.optional-dependencies]
test = ["pytest"]

[tool.setuptools]
packages = { find = {} }
