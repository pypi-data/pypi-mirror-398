[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aiogram-smart-messages"      # имя для pip install
version = "0.1.0"
description = "Smart message renderer for Aiogram with keyboard builder and JSON loader"
readme = "README.md"
license = {text = "MIT"}
authors = [{name="Arseniy", email="your.email@example.com"}]
requires-python = ">=3.11"
dependencies = [
    "aiogram>=3.0"
]