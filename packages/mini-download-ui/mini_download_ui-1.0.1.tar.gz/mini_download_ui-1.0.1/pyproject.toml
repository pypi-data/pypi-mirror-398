[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mini-download-ui"
version = "1.0.1"
description = "Minimal terminal download UI"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "Mathys B."}
]

classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
]

dependencies = []
