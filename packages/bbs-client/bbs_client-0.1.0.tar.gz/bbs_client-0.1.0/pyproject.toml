[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "bbs-client"
version = "0.1.0"
description = "MyBBS client"
requires-python = ">=3.9"

[project.scripts]
mybbs = "mybbs.bbs_client:main"
