[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "test00000000000032"
version = "0.0.1"
description = "Temporary test package for token verification"
requires-python = ">=3.7"
