[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "donapram"
version = "1.0.0"
description = "Module Python basé sur une DLL externe"
authors = [
    { name="Leonel Taffoup", email="leonelbrice@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.14"
license = { text = "MIT" }

[tool.setuptools]
packages = ["donapram"]

[tool.setuptools.package-data]
donapram = ["*.pyd"]