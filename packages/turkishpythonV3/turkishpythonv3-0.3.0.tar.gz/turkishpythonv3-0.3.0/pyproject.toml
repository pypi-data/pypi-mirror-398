
[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "turkishpythonV3"
version = "0.3.0"
description = "Python'u Türkçe yazmak için kütüphane"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
  {name = "turkishpython", email = "example@mail.com"}
]
