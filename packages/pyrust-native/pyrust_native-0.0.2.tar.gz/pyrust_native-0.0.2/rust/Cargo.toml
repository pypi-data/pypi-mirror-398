[package]
name = "pyrust-native"
version = "0.0.1"
edition = "2021"

[lib]
name = "pyrust__native"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
