[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "luxprint"
version = "1.0.0"
description = "Elegant terminal text updater and animated progress bars"
authors = [{ name = "YourName" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
keywords = ["terminal", "progress", "cli", "animation"]
