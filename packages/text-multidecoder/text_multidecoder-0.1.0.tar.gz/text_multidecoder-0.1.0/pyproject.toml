[project]
name = "text-multidecoder"
version = "0.1.0"
description = "Recursively attempt to decode text"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chardet>=5.2.0",
    "click>=8.3.1",
]

[project.scripts]
multidecoder = "multidecoder.cli:main"

[tool.uv]
package = true
