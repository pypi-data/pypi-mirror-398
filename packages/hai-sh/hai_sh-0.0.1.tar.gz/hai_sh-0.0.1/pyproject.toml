# pyproject.toml
[project]
name = "hai-sh"
version = "0.0.1"
description = "AI-powered terminal assistant that suggests or executes bash commands from natural language."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "AGPL v3"}
authors = [{name = "Frank Bria"}]
dependencies = [
    "build>=1.3.0",
    "twine>=6.2.0",
]

[project.urls]
Repository = "https://github.com/frankbria/hai"
