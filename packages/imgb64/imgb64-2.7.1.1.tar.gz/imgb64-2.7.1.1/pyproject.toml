[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "imgb64"
version = "2.7.1.1"
description = "Base64 image encode/decode"
readme = "README.md"
requires-python = ">=3.8"
authors = [{name = "Wl13Proger9"}]

[tool.setuptools.packages.find]
include = ["imgb64"]