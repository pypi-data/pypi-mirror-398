[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name ="recko-ai-sdk"
version = "0.1.2"
description = "Reckomate Python SDK"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0"
]
