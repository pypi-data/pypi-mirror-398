[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "securerun-manifest"
version = "0.1.0"
description = "Secure Run Manifest (`sr-ms`) is a lightweight Python tool to securely lock, manage, and run Python scripts."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.9"
authors = [
  { name="Viren" }
]

dependencies = []

[project.scripts]
srms = "sr_ms.srms:main"