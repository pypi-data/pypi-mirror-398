[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wiredrss"
version = "0.1.2"
description = "Typed helper for consuming WIRED’s RSS feed."
requires-python = ">=3.11"
dependencies = ["feedparser>=6.0"]

[tool.setuptools]
packages = ["wiredrss"]
