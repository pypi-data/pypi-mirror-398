#!/bin/env bash
CMD_BASE="$(readlink -f "$0" 2>/dev/null || greadlink -f "$0")" || CMD_BASE="$0"; CMD_BASE="$(dirname "$CMD_BASE")"

unset ANTHROPIC_API_KEY
exec claude --settings "$CMD_BASE/claude-code-settings-vandamme.json" "$@"
