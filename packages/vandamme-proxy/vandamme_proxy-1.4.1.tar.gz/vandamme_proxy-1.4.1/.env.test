# Test environment configuration for Vandamme Proxy
#
# NOTE: This file only contains non-sensitive configuration.
# API keys are set by the session-scoped setup_test_environment fixture in conftest.py
# This prevents conflicts and ensures consistent test environments.

# Test server settings
HOST="0.0.0.0"
PORT="8082"

# Performance settings optimized for testing
LOG_LEVEL="DEBUG"
MAX_TOKENS_LIMIT="4096"
MIN_TOKENS_LIMIT="100"
REQUEST_TIMEOUT="30"  # Shorter timeout for tests
MAX_RETRIES="1"  # Fewer retries for faster tests

# Test configuration (API keys are set by fixtures, not here)
# See tests/conftest.py and tests/config/test_config.py for API key values