[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tech-sentiment-analyzer"
version = "2025.12.21152823"
description = "A new package that analyzes text input to determine sentiment intensity and polarity toward specific tech CEOs or companies, returning structured scores and rankings. It uses pattern matching to ensur"
readme = "README.md"
authors = [{ name = "tech-sentiment-analyzer", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/tech-sentiment-analyzer"
