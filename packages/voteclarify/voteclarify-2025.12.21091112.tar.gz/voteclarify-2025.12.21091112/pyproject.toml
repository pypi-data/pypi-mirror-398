[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "voteclarify"
version = "2025.12.21091112"
description = "A new package that processes user queries about why small voting or ranking projects get flagged as spam so easily. It uses natural language processing to understand the input and generates a structur"
readme = "README.md"
authors = [{ name = "voteclarify", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/voteclarify"
