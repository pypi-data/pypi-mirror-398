name: Setup UV and install dependencies
description: Installs uv and syncs project dependencies
runs:
  using: composite
  steps:
    - name: Install uv
      shell: bash
      run: curl -LsSf https://astral.sh/uv/install.sh | sh

    - name: Install dependencies
      shell: bash
      run: uv sync
