[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qetoo"
version = "0.1.0"
description = "Simple browser opening helper"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "YourName" }
]
dependencies = [
  "playwright"
]
