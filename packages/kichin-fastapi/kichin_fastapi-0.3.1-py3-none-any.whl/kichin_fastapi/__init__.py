"""Kichin FastAPI Library.

This library provides models and utilities for the Kichin data distribution framework.
---

For models see the kichin_fastapi.models module.

Example:
    >>> from kichin_fastapi.models import Dataset
    >>> dataset = Dataset(id="test", name="Test Dataset", slug="test-dataset")
    >>> print(dataset.id)
    'test'
"""
