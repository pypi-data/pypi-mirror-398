name: Run Unit Tests
description: Run unit tests with pytest

runs:
  using: composite
  steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Hatch
      uses: ./.github/actions/setup-hatch

    - name: Run unit tests
      shell: bash
      run: hatch run dev:pytest tests/ -v
