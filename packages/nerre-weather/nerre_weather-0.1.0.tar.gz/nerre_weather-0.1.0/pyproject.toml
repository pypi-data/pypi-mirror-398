[project]
name = "nerre_weather"
version = "0.1.0"
description = "Nerre multi-city weather CLI with charts and CSV export"
readme = "README.md"
authors = [
  { name = "Nerre", email = "your.email@example.com" }
]
requires-python = ">=3.9"
dependencies = [
  "requests",
  "tabulate",
  "matplotlib",
  "colorama"
]

[project.scripts]
nerreweather = "nerre_weather.cli:main"

[tool.setuptools.packages.find]
where = ["."]
