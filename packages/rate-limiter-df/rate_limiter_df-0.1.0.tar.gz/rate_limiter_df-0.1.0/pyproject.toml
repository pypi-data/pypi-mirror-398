[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rate-limiter-df"
version = "0.1.0"
requires-python = ">=3.7"
description = "A decorator-based rate limiter using the token bucket algorithm"
readme = "README.md"
authors = [
    {name = "Dan Foster"}
]
maintainers = [
    {name = "Dan Foster"}
]
license = {file = "LICENSE.txt"}
keywords = [
    "RateLimiter",
    "Rate",
    "Limiter"
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.7",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

[project.urls]
Homepage = "https://github.com/yourusername/rate-limiter-df"
Repository = "https://github.com/yourusername/rate-limiter-df"

[tool.setuptools]
packages = ["rate_limiter_df"]

[tool.pytest.ini_options]
testpaths = ["rate_limiter_df/tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

[tool.mypy]
python_version = "3.11"
strict = true
exclude = '(build|dist|tests|examples)/'

[project.optional-dependencies]
dev = [
    "mypy",
    "ruff",
    "pytest",
]

