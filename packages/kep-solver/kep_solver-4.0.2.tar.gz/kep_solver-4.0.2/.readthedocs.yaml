version: 2

sphinx:
   configuration: docs/conf.py

# Need to use testing image for python 3.9
build:
   os: ubuntu-20.04
   tools:
       python: "3.10"

python:
   install:
     - path: .
       extra_requirements:
         - docs
