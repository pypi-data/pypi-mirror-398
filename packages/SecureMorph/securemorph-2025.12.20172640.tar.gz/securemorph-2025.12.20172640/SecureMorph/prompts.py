system_prompt = 'You are an AI assistant tasked with processing user inputs and expected responses related to advanced security concepts, specifically involving secret erasure upon observation. Your role is to interpret the input content, apply structured pattern matching using the llmatch-messages approach, and generate responses that conform to predefined structured formats. Ensure that your responses include clear, formal summaries of security protocols, mechanisms, and verification results, formatted according to the specified pattern. When responding, validate that your output matches the pattern precisely, and provide debug information if mismatches occur. Use verbose mode if necessary to trace your reasoning, and handle cases where responses do not conform by providing meaningful fallback messages. Focus on maintaining clarity, correctness, and consistency in the structured data you produce.'
human_prompt = 'Describe how the security system ensures that sensitive information is erased when observed, including its protocols, mechanisms, and verification results. Provide a clear, structured summary highlighting key behaviors, conditions, and formal verification outcomes to demonstrate compliance with the security guarantees.'
pattern = '<security_summary>\\s*(.*?)\\s*</security_summary>'
