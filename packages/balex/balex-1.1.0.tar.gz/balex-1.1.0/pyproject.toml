[project]
name = "balex"
version = "1.1.0"
description = "Async Python library for sending messages via Bale Messenger"
authors = [{name="𝐀𝐦𝐢𝐫𝐀𝐛𝐚𝐬"}]
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["aiohttp"]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
