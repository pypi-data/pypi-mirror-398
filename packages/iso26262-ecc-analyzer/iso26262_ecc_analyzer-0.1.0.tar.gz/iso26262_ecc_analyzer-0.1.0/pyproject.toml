[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "iso26262-ecc-analyzer"
version = "0.1.0"
description = "Safety analysis and visualization tool according to ISO 26262."
readme = "README.md"
authors = [
  { name = "Linus Held", email = "your.email@example.com" },
]
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = [
    "graphviz>=0.20",
]

[project.urls]
"Homepage" = "https://github.com/HENKI2004/iso26262-ecc-analyzer" 
"Bug Tracker" = "https://github.com/HENKI2004/iso26262-ecc-analyzer/issues"
"Documentation" = "https://henki2004.github.io/iso26262-ecc-analyzer/"

[project.optional-dependencies]
dev = [
    "pytest",
    "black",
    "ruff",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 200
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.format]
quote-style = "double"