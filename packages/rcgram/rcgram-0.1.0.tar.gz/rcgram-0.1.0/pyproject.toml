[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rcgram"
version = "0.1.0"
description = "Compact, simple Telegram Bot libary."
dependencies = [
    "requests",
]
