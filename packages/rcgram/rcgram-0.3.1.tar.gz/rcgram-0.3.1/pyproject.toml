[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rcgram"
version = "0.3.1"
description = "Async Telegram library with direct access."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "httpx",
]
