[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rcgram"
version = "0.3.0.post1"
description = "Async Telegram library using httpx."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "httpx",
]
