[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tp-llmatch-formatter"
version = "2025.12.21170024"
description = "A new package designed to streamline content management tasks within Textpattern CMS 4.9.0. It allows users to input text-based content or instructions related to their CMS, such as article drafts, fo"
readme = "README.md"
authors = [{ name = "tp-llmatch-formatter", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/tp-llmatch-formatter"
