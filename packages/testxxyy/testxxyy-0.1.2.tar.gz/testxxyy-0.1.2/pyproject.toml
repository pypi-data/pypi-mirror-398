[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "testxxyy"
version = "0.1.2"
description = "A simple test library"
requires-python = ">=3.7"