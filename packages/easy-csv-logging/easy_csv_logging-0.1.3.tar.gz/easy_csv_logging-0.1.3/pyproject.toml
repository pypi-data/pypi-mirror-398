[project]
name = "easy-csv-logging"
version = "0.1.3"
description = "Simple CSV logging and viewing"
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
authors = [{ name = "Anton Axelson" }]

dependencies = [
    "pandas",
    "rich"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"