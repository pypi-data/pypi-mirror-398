[project]
name = "easy-csv-logging"
version = "0.1.0"
description = "Simple and easy CSV logging"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{name = "Anton Axelson"}]
dependencies = [
    "pandas",
    "rich"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"