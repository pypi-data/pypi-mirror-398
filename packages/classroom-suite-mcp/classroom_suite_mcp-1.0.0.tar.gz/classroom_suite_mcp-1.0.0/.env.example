# Google OAuth credentials file path (download from Google Cloud Console)
GOOGLE_CREDENTIALS_PATH=credentials.json

# Token storage path (auto-generated after first auth)
GOOGLE_TOKEN_PATH=token.json
