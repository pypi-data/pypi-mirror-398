[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "termdeployr-llmdeploy-stepdeploy-cliassist-deployflow-termguide-llmdeployer-deploymate-terminaldeploy-smartdeploy"
version = "2025.12.21142331"
description = "A new package that helps users deploy applications by providing structured, step-by-step guidance through terminal commands. Users input their deployment goals or issues, and the package uses llmatch-"
readme = "README.md"
authors = [{ name = "termdeployr-llmdeploy-stepdeploy-cliassist-deployflow-termguide-llmdeployer-deploymate-terminaldeploy-smartdeploy", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell"
