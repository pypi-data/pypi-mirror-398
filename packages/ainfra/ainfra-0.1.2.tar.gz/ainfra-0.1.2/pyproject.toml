[tool.poetry]
name = "ainfra"
version = "0.1.2"
description = "Simple way to pip install torch, vllm, flash-attn, sglang, ...."
authors = ["LinXueyuanStdio <23211526+LinXueyuanStdio@users.noreply.github.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.scripts]
ainfra = "ainfra.cli:app"

[tool.poetry.dependencies]
typer = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
