
[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sataid"
version = "0.1.3"
description = "Read and process SATAID meteorological satellite data"
authors = [{ name="Sepriando", email="meteo.go@gmail.com" }]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.7"
