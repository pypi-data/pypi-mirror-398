[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Jeasydata"
version = "0.1.1"
description = "Janek's easy data reader/writer"
readme = "README.md"
requires-python = ">=3.8"
authors = [{ name = "janektech.pl" }]
license = { text = "MIT" }
dependencies = []

[tool.setuptools]
package-dir = {"" = "."}

[tool.setuptools.packages.find]
where = ["."]
