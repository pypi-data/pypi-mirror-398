[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cpp26-equality-linter"
version = "2025.12.20185334"
description = "A new package that automates equality checks in C++ code by leveraging the upcoming reflection capabilities in C++26. This tool will allow developers to input C++ code snippets or class definitions, a"
readme = "README.md"
authors = [{ name = "cpp26-equality-linter", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/cpp26-equality-linter"
