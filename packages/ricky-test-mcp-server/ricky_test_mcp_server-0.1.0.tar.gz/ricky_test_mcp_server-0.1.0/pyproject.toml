[project]
name = "ricky-test-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "yikan", email = "yikanwang557@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.25.0",
]

[project.scripts]
ricky-test-mcp-server = "ricky_test_mcp_server:main"

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
