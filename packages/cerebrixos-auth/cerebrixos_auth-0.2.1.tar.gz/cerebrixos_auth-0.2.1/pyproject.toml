
[project]
name = "cerebrixos-auth"
version = "0.2.1"
description = "CerebrixOS OIDC/JWT auth helpers (test + prod)"
authors = [{name="CerebrixOS", email="dev@cerebrixos.com"}]
requires-python = ">=3.9"
dependencies = ["requests>=2.31", "python-jose[cryptography]>=3.3.0"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
