{
  "title": "GraphPatch",
  "type": "object",
  "additionalProperties": false,
  "required": ["run_id", "base_revision", "patch"],
  "properties": {
    "run_id": { "type": "string" },
    "base_revision": { "type": "integer", "minimum": 0 },
    "patch": { "type": "object", "additionalProperties": true },
    "extensions": { "$ref": "../common/extensions.schema.json" }
  }
}

