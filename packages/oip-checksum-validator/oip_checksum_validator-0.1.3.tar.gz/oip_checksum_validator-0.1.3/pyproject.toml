[project]
name = "oip-checksum-validator"
version = "0.1.3"
requires-python = ">=3.12"
dependencies = ["blake3>=1.0.8"]
classifiers = [
    "Programming Language :: Python :: 3.12",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: Implementation :: CPython",
]
readme = "README.md"

[project.urls]
Homepage = "https://openinnovation.ai/"

[project.scripts]
oic = "main:main"

[dependency-groups]
dev = ["ruff>=0.14.5"]
