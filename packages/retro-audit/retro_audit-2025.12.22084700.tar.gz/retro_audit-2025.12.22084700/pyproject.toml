[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "retro-audit"
version = "2025.12.22084700"
description = "retro-audit helps gamers craft authentic, spam‑free retro content with structured feedback for better engagement."
readme = "README.md"
authors = [{ name = "retro-audit", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/retro-audit"
