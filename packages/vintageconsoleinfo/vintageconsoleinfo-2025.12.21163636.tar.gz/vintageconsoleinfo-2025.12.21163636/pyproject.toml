[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vintageconsoleinfo"
version = "2025.12.21163636"
description = "The system is designed to process user descriptions or inputs related to vintage gaming consoles, such as the Interton Video Computer 4000, and generate structured summaries or specifications. It focu"
readme = "README.md"
authors = [{ name = "vintageconsoleinfo", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/vintageconsoleinfo"
