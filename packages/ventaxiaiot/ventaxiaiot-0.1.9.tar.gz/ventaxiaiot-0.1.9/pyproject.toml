[project]
name = "ventaxiaiot"
version = "0.1.9"
description = "Python API for controlling Vent-Axia IoT devices"
authors = [{ name = "josyban" }]
readme = "README.md"
license = {text = "MIT"}  
requires-python = ">=3.13"
dependencies = []

[project.scripts]
ventaxiaiot = "ventaxiaiot.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
