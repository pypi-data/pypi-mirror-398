[project]
name = "arcbeam-connector"
version = "0.0.2"
description = "Arcbeam connector for AI frameworks."
readme = "README.md"
authors = [
    { name = "Arcbeam Eng", email = "arcbeameng@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "opentelemetry-exporter-otlp>=1.39.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
