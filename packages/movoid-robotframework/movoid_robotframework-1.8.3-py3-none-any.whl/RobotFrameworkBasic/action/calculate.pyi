##################################################
# This is auto generated by code.
##################################################

import math
import random
import RobotFrameworkBasic.basic

class ActionCalculate(RobotFrameworkBasic.basic.Basic):
	calculate_get_random_int: staticmethod
	def __init__(self):
		super().__init__()
	def calculate_get_random(self, random_max = 1, random_min = 0, digit = 3, _return_when_error = None, _log_keyword_structure = True, _return_name = None, _show_return_info = None, _simple_doc = False, _debug_default = None, _debug_debug = None, _force_raise = None):
		"""
		get random with a digit
		:param random_max: max
		:param random_min: min,change to max if is more than max
		:param digit: digit
		:return: a random float
		:param _return_when_error: 输入任意非None值后，当error发生时，不再raise error，而是返回这个值
		:param _log_keyword_structure: bool : 默认True，生成一组robotframework格式的可展开的日志。如果False时，就不会把这个函数做成折叠状，而是只打印一些内容
		:param _return_name: str : 你可以把代码中这个函数赋值的变量str写在这儿，来让日志更加贴近python代码内容
		:param _show_return_info: bool :默认True，是否把return的信息打印出来。
		:param _simple_doc: bool :默认False，是否仅打印第一行doc信息
		:param _debug_default:在不唤醒UI时，遇上error的处理逻辑，0/1为上报错误；2为跳过错误；默认1,
		:param _debug_debug：在会唤醒UI时，遇上error的处理逻辑，0为弹出UI进行处理；1为不弹出UI并向上报错；2为不弹出UI，也不向上报错；默认为0
		:param _force_raise：设置为True后，可以让它的所有的子函数全部都主动raise error，而不是弹出窗口或跳过错误；默认为False
		"""
		...
	def calculate_average_similar(self, number1, number2, accuracy = 0.001, _return_when_error = None, _log_keyword_structure = True, _return_name = None, _show_return_info = None, _simple_doc = False, _debug_default = None, _debug_debug = None, _force_raise = None):
		"""
		计算两个数值或数组的平均值是否足够相近
		:param number1:
		:param number2:
		:param accuracy: 数小，就判定两个数值的绝对差值的相似阈值。如果数字比accuracy大，那就是两者的百分比差距阈值。只要有一个方向上满足了就是True
		:return:true or false。两者是否相近
		:param _return_when_error: 输入任意非None值后，当error发生时，不再raise error，而是返回这个值
		:param _log_keyword_structure: bool : 默认True，生成一组robotframework格式的可展开的日志。如果False时，就不会把这个函数做成折叠状，而是只打印一些内容
		:param _return_name: str : 你可以把代码中这个函数赋值的变量str写在这儿，来让日志更加贴近python代码内容
		:param _show_return_info: bool :默认True，是否把return的信息打印出来。
		:param _simple_doc: bool :默认False，是否仅打印第一行doc信息
		:param _debug_default:在不唤醒UI时，遇上error的处理逻辑，0/1为上报错误；2为跳过错误；默认1,
		:param _debug_debug：在会唤醒UI时，遇上error的处理逻辑，0为弹出UI进行处理；1为不弹出UI并向上报错；2为不弹出UI，也不向上报错；默认为0
		:param _force_raise：设置为True后，可以让它的所有的子函数全部都主动raise error，而不是弹出窗口或跳过错误；默认为False
		"""
		...
	def calculate_average_similar_in_digit(self, number1, number2, digit = 3, offset_max = 1.001, _return_when_error = None, _log_keyword_structure = True, _return_name = None, _show_return_info = None, _simple_doc = False, _debug_default = None, _debug_debug = None, _force_raise = None):
		"""
		计算两组数之间，在某一位之前是否完全
		:param number1: 第一组数/数组
		:param number2: 第二组数/数组
		:param digit: 小数点后位数
		:param offset_max: 允许的误差极限，如果是digit=3，offset_max=1.001，意味着允许两个数值有最多1.001e-3的差值
		:return: true or false。两者是否相近
		:param _return_when_error: 输入任意非None值后，当error发生时，不再raise error，而是返回这个值
		:param _log_keyword_structure: bool : 默认True，生成一组robotframework格式的可展开的日志。如果False时，就不会把这个函数做成折叠状，而是只打印一些内容
		:param _return_name: str : 你可以把代码中这个函数赋值的变量str写在这儿，来让日志更加贴近python代码内容
		:param _show_return_info: bool :默认True，是否把return的信息打印出来。
		:param _simple_doc: bool :默认False，是否仅打印第一行doc信息
		:param _debug_default:在不唤醒UI时，遇上error的处理逻辑，0/1为上报错误；2为跳过错误；默认1,
		:param _debug_debug：在会唤醒UI时，遇上error的处理逻辑，0为弹出UI进行处理；1为不弹出UI并向上报错；2为不弹出UI，也不向上报错；默认为0
		:param _force_raise：设置为True后，可以让它的所有的子函数全部都主动raise error，而不是弹出窗口或跳过错误；默认为False
		"""
		...
	def calculate_each_similar_in_digit(self, list1, list2, digit = 3, offset_max = 1.001, _return_when_error = None, _log_keyword_structure = True, _return_name = None, _show_return_info = None, _simple_doc = False, _debug_default = None, _debug_debug = None, _force_raise = None):
		"""
		计算两组数之间，在某一位之前是否完全
		:param list1: 第一组数组
		:param list2: 第二组数组
		:param digit: 小数点后位数
		:param offset_max: 允许的误差极限，如果是digit=3，offset_max=1.001，意味着允许两个数值有最多1.001e-3的差值
		:return: true or false。两者是否相近
		:param _return_when_error: 输入任意非None值后，当error发生时，不再raise error，而是返回这个值
		:param _log_keyword_structure: bool : 默认True，生成一组robotframework格式的可展开的日志。如果False时，就不会把这个函数做成折叠状，而是只打印一些内容
		:param _return_name: str : 你可以把代码中这个函数赋值的变量str写在这儿，来让日志更加贴近python代码内容
		:param _show_return_info: bool :默认True，是否把return的信息打印出来。
		:param _simple_doc: bool :默认False，是否仅打印第一行doc信息
		:param _debug_default:在不唤醒UI时，遇上error的处理逻辑，0/1为上报错误；2为跳过错误；默认1,
		:param _debug_debug：在会唤醒UI时，遇上error的处理逻辑，0为弹出UI进行处理；1为不弹出UI并向上报错；2为不弹出UI，也不向上报错；默认为0
		:param _force_raise：设置为True后，可以让它的所有的子函数全部都主动raise error，而不是弹出窗口或跳过错误；默认为False
		"""
		...
	...

