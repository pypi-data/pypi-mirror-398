image: python:3.10-buster

stages:
  - deploy

before_script:
   - pip install flit

deploy_production:
  stage: deploy
  script:
    - flit build --no-use-vcs
  only:
    - master
  cache:
    paths:
      - dist/
  artifacts:
    paths:
      - dist/
