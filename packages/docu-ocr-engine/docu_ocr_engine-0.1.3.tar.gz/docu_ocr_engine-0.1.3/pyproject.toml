[project]
name = "docu-ocr-engine"
version = "0.1.3"
description = "Structured OCR extraction engine"
authors = [{name = "Your Name", email = "your@email.com"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "fastapi",
    "httpx",
    "tenacity"
]
# Add any other runtime dependencies here
 
[project.optional-dependencies]
dev = [
    "pytest",
    "flake8",
    "black",
    "isort"
]
# Add any other runtime dependencies here

[tool.setuptools]
packages = ["docu_ocr"]

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
