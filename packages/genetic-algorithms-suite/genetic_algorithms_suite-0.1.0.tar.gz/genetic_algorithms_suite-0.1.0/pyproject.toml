[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "genetic-algorithms-suite"
version = "0.1.0"
description = "A collection of genetic algorithm implementations for common optimization problems."
readme = "README.md"
authors = [{ name="Your Name", email="your.email@example.com" }]
license = { file="LICENSE" }
requires-python = ">=3.8"

dependencies = [
    "numpy>=1.21"
]

[project.urls]
Homepage = "https://github.com/yourusername/genetic-algorithms-suite"
