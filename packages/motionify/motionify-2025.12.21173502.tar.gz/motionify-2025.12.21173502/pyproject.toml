[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "motionify"
version = "2025.12.21173502"
description = "A new package designed to facilitate the creation and control of animated characters in real-world environments by interpreting user instructions and transforming them into structured action plans. It"
readme = "README.md"
authors = [{ name = "motionify", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/motionify"
