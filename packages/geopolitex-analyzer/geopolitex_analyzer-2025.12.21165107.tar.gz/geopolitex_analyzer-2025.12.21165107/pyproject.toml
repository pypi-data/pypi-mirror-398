[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "geopolitex-analyzer"
version = "2025.12.21165107"
description = "A new package that processes news headlines or short text inputs related to geopolitical and defense topics, such as reports on military capabilities or international competition. It uses an LLM to ge"
readme = "README.md"
authors = [{ name = "geopolitex-analyzer", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/geopolitex-analyzer"
