[build-system]
requires = ["setuptools>=64", "setuptools-scm>=8", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "miriel-python"
dynamic = ["version", "readme"]
description = "Library for interacting with the Miriel API"
authors = [{ name = "Andy Barkett", email = "andy@miriel.ai" }]
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = [
    "requests>=2.0",
    "python-json-logger>=2.0.7"  # added: installs automatically at runtime
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.optional-dependencies]
dev = [
    "build>=1.0",
    "pytest>=7.0",
    "ruff",
]

[tool.setuptools_scm]
version_scheme = "guess-next-dev"
local_scheme = "node-and-date"

[tool.setuptools.dynamic]
readme = {file = ["README.md"], content-type = "text/markdown"}

[tool.setuptools.packages.find]
exclude = ["tests", "examples"]

[project.urls]
Homepage = "https://github.com/miriel-ai/miriel-python"

[tool.ruff]
line-length = 120

[tool.ruff.format]
quote-style = "single"
indent-style = "space"

[tool.pytest.ini_options]
log_level = "DEBUG"
