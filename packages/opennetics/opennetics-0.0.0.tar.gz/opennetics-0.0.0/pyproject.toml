[build-system]
requires =  ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "opennetics"
version = "0.0.0"
authors = [
    { name="Utkarsh Ranjan", email="utkarsh@cyberfear.com" }
]

description = ""
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent"
]

[project.urls]
Homepage = "https://github.com/OpenNetics"
Issues = "https://github.com/OpenNetics"

[tool.hatch.build.targets.wheel]
packages = ["src/opennetics"]

