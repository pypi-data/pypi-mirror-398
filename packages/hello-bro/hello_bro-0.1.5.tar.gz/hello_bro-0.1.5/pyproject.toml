[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hello-bro"
version = "0.1.5"
description = "A minimal hello world library"
authors = [
    { name = "bro" }
]
readme = "README.md"
requires-python = ">=3.8"


[tool.setuptools]
packages = ["hello"]
