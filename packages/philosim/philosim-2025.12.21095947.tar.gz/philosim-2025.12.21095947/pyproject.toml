[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "philosim"
version = "2025.12.21095947"
description = "A new package that leverages the power of llmatch-messages to provide structured insights into complex philosophical and scientific topics, such as the simulation hypothesis. Users can input a specifi"
readme = "README.md"
authors = [{ name = "philosim", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/philosim"
