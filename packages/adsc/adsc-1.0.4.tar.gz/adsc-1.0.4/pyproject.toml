[project]
name = "adsc"
version = "1.0.4"
description = "AdsC Telegram multi-account broadcast bot"
readme = "README.md"
requires-python = ">=3.9"

authors = [
  { name="AdsC", email="adsc.bot@gmail.com" }
]

dependencies = [
  "telethon>=1.34.0",
  "python-telegram-bot==20.7"
]

[project.scripts]
adsc = "adsc.main:run"
