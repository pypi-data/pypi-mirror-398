[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "adsc"
version = "1.0.3"
description = "AdsC Telegram broadcast bot"
readme = "README.md"
requires-python = ">=3.9"

authors = [
  { name="AdsC", email="adsc.bot@gmail.com" }
]

dependencies = []

[project.scripts]
adsc = "adsc.main:run"
