# Coarse sigma sweep - wide range exploration
# Use this to find the approximate optimal sigma region
# Note: sigma=1 is too narrow for effective optimization

enabled: true
sigmas: [2, 3, 4, 5, 7, 10]
description: "Coarse sweep: wide sigma range to identify optimal region"
