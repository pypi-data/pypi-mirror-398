[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "devide-spec"
version = "2025.12.21174359"
description = "A new package that transforms unstructured developer tool ideas from text inputs into structured, actionable product specifications. Users can describe their desired tool in natural language, and the"
readme = "README.md"
authors = [{ name = "devide-spec", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/devide-spec"
