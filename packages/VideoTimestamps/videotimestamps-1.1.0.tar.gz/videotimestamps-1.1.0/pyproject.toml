[project]
name = "VideoTimestamps"
description = "Get video timestamps."
authors = [
    { name = "moi15moi", email = "moi15moismokerlolilol@gmail.com" },
]
license = "MIT"
license-files = ["LICENSE"]
readme = "README.md"
requires-python = ">=3.10"
classifiers=[
        "Development Status :: 5 - Production/Stable",
        "Intended Audience :: Developers",
        "Programming Language :: Python",
        "Programming Language :: Python :: 3",
        "Programming Language :: Python :: 3.10",
        "Programming Language :: Python :: 3.11",
        "Programming Language :: Python :: 3.12",
        "Programming Language :: Python :: 3.13",
        "Programming Language :: Python :: 3.14",
        "Typing :: Typed",
]
dynamic = ["version"]

[project.scripts]
extracttimestamps = "video_timestamps.extract_timestamps:main"

[project.urls]
Documentation = "https://moi15moi.github.io/VideoTimestamps/"
Source = "https://github.com/moi15moi/VideoTimestamps/"
Tracker = "https://github.com/moi15moi/VideoTimestamps/issues/"

[project.optional-dependencies]
dev = [
  "coverage>=7.0.0",
  "mypy>=1.0.0",
  "pillow>=10.0.0",
  "pytest>=8.0.0",
  "ruff>=0.8.4",
  "mkdocs",
  "mkdocs-material",
  "mkdocstrings[python]",
]

[build-system]
requires = ["meson", "meson-python"]
build-backend = "mesonpy"

[tool.meson-python.args]
# From https://mesonbuild.com/meson-python/how-to-guides/shared-libraries.html#static-library-from-subproject
setup = [
  "--default-library=static",
  "--wrap-mode=forcefallback",
  "-Dbestsource:enable_plugin=false",
]
install = ['--skip-subprojects']

[tool.mypy]
exclude = ["build", "subprojects"]
files = ["."]
warn_unused_configs = true
strict = true

[tool.coverage.report]
exclude_also = [
  # Don't complain about abstract methods, they aren't run:
  "@(abc\\.)?abstractmethod",
]

[tool.ruff.lint]
exclude = ["__init__.py"]

[tool.pytest.ini_options]
addopts = "--ignore=subprojects/"

[tool.cibuildwheel.macos]
environment = { MACOSX_DEPLOYMENT_TARGET="10.15" }
