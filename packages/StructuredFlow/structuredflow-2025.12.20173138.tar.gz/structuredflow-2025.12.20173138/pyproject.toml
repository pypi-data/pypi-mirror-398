[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "StructuredFlow"
version = "2025.12.20173138"
description = "A new package designed to facilitate structured, reliable interactions with language models for summarizing and analyzing user input text. It receives raw or pre-processed text data as input, applies "
readme = "README.md"
authors = [{ name = "StructuredFlow", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/StructuredFlow"
