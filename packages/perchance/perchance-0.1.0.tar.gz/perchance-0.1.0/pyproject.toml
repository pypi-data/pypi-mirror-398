[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "perchance"
version = "0.1.0"
homepage = "https://github.com/eeemoon/perchance"
description = "Unofficial Python API for Perchance."
authors = ["EeeMoon"]
license = "MIT"
readme = "README.md"
include = ["pyproject.toml", "LICENSE"]

[tool.poetry.dependencies]
python = "^3.11"
aiofiles = "^25.1.0"
playwright = "^1.57.0"
