[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ngbs-icon"
version = "0.2.0"
description = "Async Python client for NGBS iCON controllers"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "Your Name" }
]

dependencies = []

[project.urls]
Homepage = "https://github.com/yourname/ngbs-icon"
