"""
GUI API 模块

提供与 MCP 工具相同逻辑的 REST API，供 GUI 使用。
注意：实际 API 已集成在 server.py 中，此文件仅作为模块占位。
"""

# GUI API 端点已在 server.py 中实现：
# - GET /api/servers - 获取所有 server
# - GET /api/servers/<name> - 获取 server 详情
# - GET /api/skills - 获取所有 skill
# - GET /api/skills/<name> - 获取 skill 详情
# - POST /api/match-skill - 匹配 skill

# 这些 API 与 MCP 工具共享相同的底层逻辑，
# 确保 GUI 展示的内容与 LLM 获取的完全一致。

