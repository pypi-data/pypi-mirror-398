[build-system]
requires = ["flit_core >=3.11,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "paginators"
readme = "README.rst"
authors = [{name = "kaleissin", email = "kaleissin@gmail.com"}]
license = "MIT"
license-files = ["LICENSE"]
dynamic = ["version", "description"]
requires-python = ">= 3.0"
dependencies = ["Django"]

[project.urls]
Source = "https://codeberg.org/Kaleissin/django-paginators"
