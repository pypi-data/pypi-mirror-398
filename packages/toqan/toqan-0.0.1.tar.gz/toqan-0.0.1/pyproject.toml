[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "toqan"
version = "0.0.1"
authors = [
  { name="CK", email="author@example.com" },
]
description = "toqan package"
readme = "README.md"
requires-python = ">=4.0"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]
