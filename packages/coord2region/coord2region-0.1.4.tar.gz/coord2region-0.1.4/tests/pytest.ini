[pytest]
markers =
    requires_network: mark tests that require network access
    slow: mark tests that are slow to run
    integration: mark integration tests
    unit: mark unit tests
testpaths = tests
