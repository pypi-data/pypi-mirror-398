[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "go-concise-summary"
version = "2025.12.21141123"
description = "A new package would take a technical description or code snippet related to concurrency in Go and generate a structured summary of the concept, such as a fair, cancelable semaphore. It would extract k"
readme = "README.md"
authors = [{ name = "go-concise-summary", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/go-concise-summary"
