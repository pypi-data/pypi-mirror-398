[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lego-museum-structurizer"
version = "2025.12.20202603"
description = "The package gives users a simple, text‑based interface for turning their free‑form imagination about a secret Lego museum into a machine‑friendly summary. A user writes a brief description of the scen"
readme = "README.md"
authors = [{ name = "lego-museum-structurizer", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/lego-museum-structurizer"
