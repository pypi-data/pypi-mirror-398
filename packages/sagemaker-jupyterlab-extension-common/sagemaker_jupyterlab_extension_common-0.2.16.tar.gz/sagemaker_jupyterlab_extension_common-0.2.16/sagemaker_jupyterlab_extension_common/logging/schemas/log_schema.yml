$id: http://sagemaker.studio.jupyterserver.log.schema
version: 1
type: object
properties:
  Level:
    description: |
      The log level associated with the event
    type: string
  Message:
    description: |
      The log message associated with the event
    type: string
  Name:
    description: |
      The name of the error
    type: string
  Stack:
    description: |
      The stacktrace of the error
    type: string
  Context:
    type: object
    properties:
      AccountId:
        description: |
          The AWS account ID associated with the event
        type: string
      DomainId:
        description: |
          The domain ID associated with the event
        type: string
      UserProfileName:
        description: |
          The user profile name associated with the request
        type: string
      SpaceName:
        description: |
          The name that identifies current space
        type: string
      Component:
        description: |
          The component where the log is emitted from (e.g. the name of the API handler)
        type: string
      InternetAccessType:
        description: |
          Internet access type, VPC only or Public internet
        type: string
      ExtensionName:
        description: |
          Name of the extension, where the log event is emitted from
        type: string
      ExtensionVersion:
        description: |
          Version number of the extension
        type: string
      ClientRequestId:
        description: |
          Unique client request id (see Client Request section for details), generated on client side upon HTTP request
        type: string
      ServerRequestId:
        description: |
          Unique server request id, generated on server side upon HTTP request
        type: string
    required:
      - AccountId
      - DomainId
      - SpaceName
      - ExtensionName
      - ExtensionVersion
required:
  - Level
  - Message
  - Context
