[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kiphellopip"
version = "0.0.1"
description = "My first pip module upload drive"
readme = "README.md"
requires-python = ">=3.8"
authors = [
    { name="kip", email="kkipngenokoech22@gmail.com" },
]

[tool.setuptools.packages.find]
where = ["src"]