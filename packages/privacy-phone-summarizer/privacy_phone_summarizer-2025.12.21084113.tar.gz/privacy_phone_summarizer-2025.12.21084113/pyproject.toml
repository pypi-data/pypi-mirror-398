[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "privacy-phone-summarizer"
version = "2025.12.21084113"
description = "A new package that receives a short textual description of a phone company that deliberately avoids collecting personal data and returns a structured, machine‑readable summary (e.g., JSON) of the serv"
readme = "README.md"
authors = [{ name = "privacy-phone-summarizer", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/privacy-phone-summarizer"
