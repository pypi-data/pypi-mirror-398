[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kafka-ops-guide"
version = "2025.12.20185738"
description = "A new package that provides structured, reliable summaries of technical concepts related to Kafka operations, such as partition management. It takes user-provided text (e.g., a query about increasing "
readme = "README.md"
authors = [{ name = "kafka-ops-guide", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/kafka-ops-guide"
