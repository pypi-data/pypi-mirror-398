"""{{ resource.class_name }} resource exports."""

from __future__ import annotations

import importlib
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from f5xc_py_substrate.resources.{{ resource.name }}.models import *
    from f5xc_py_substrate.resources.{{ resource.name }}.resource import {{ resource.class_name }}Resource

# Cache for lazy-loaded modules
_models_module = None


def __getattr__(name: str):
    """Lazy load exports."""
    global _models_module

    if name == "{{ resource.class_name }}Resource":
        from f5xc_py_substrate.resources.{{ resource.name }}.resource import {{ resource.class_name }}Resource
        return {{ resource.class_name }}Resource

    # Load models module once and cache it
    if _models_module is None:
        _models_module = importlib.import_module(
            "f5xc_py_substrate.resources.{{ resource.name }}.models"
        )

    # Try to get attribute from models
    try:
        return getattr(_models_module, name)
    except AttributeError:
        pass

    raise AttributeError(f"module 'f5xc_py_substrate.resources.{{ resource.name }}' has no attribute '{name}'")


__all__ = [
    "{{ resource.class_name }}Resource",
{% for model in resource.models %}
    "{{ model.class_name }}",
{% endfor %}
{% if resource.models %}
    "Spec",
{% endif %}
]
