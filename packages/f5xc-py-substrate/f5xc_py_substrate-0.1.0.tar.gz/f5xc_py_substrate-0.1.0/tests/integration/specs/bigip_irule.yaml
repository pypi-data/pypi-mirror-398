resource: bigip_irule
is_namespaced: true
dependencies: []
status: generated
notes: Auto-generated from API docs
spec:
  create:
    code: when DNS_REQUEST {if { [IP::addr [IP::remote_addr] equals 10.0.0.1/24] }
      { cname cname.siterequest.com } else { host 10.0.0.1}}
    source: AI
    irule_name: HTTP routing irule
  replace:
    code: when DNS {if { [IP::addr [IP::remote_addr] equals 10.0.0.1/24] } { cname
      cname.siterequest.com } else { host 10.0.0.1}}
    source: AI
    irule_name: HTTP routing irule
    description: SDK test - replaced
