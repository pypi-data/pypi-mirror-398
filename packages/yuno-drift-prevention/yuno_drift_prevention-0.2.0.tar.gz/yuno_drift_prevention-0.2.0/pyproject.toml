[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yuno_drift_prevention"            # Must be unique on PyPI
version = "0.2.0"
description = "A sample Python SDK"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Lukasz", email = "you@example.com"}]
requires-python = ">=3.8"
dependencies = []

[project.urls]
Homepage = "https://github.com/yourusername/my-sdk"
