[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "aws-s3-risk-footprint"
version = "0.1.5"
description = "CLI tool to analyze AWS S3 regional distribution and security risk"
authors = [{ name = "John Ward" }]
dependencies = ["boto3"]
readme = "README.md"
license = { text = "MIT" }

[project.scripts]
aws-s3-risk = "wherearemybuckets.cli:main"