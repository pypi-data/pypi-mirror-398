[project]
name = "gestion_benefices_magasin"
version = "0.1.0"
description = "Application Python orientée objet pour calculer les bénéfices des ventes d'un magasin"
authors = [
    { name = "Jassen", email = "jasseneugene@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.10"

dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
