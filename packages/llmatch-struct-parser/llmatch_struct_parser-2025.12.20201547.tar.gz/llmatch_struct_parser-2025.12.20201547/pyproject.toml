[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "llmatch-struct-parser"
version = "2025.12.20201547"
description = "A new package designed to enhance structured text processing by leveraging the capabilities of llmatch-messages. This package will take user-provided text as input and return a well-organized, structu"
readme = "README.md"
authors = [{ name = "llmatch-struct-parser", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/llmatch-struct-parser"
