[project]
name = "koruspy"
version = "0.3.2"
description = "Uma biblioteca inspirada em Rust e Kotlin para lidar com Option, println colorido e utilitários."
authors = [
    { name="Leonardo", email="leoGitKotDev@gmail.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]

[tool.flit.module]
name = "koruspy"

[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"
