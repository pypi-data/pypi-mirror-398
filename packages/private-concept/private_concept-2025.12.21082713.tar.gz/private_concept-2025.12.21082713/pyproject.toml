[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "private-concept"
version = "2025.12.21082713"
description = "A new package that enables users to provide simple text inputs about innovative, privacy-focused services—such as a phone company that doesn't collect personal data—and receive structured summaries or"
readme = "README.md"
authors = [{ name = "private-concept", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/private-concept"
