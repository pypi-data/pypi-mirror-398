[project]
name = "mathyBJS"
version = "0.1.0"
description = "NOTHING FOR NOW.."
readme = "README.md"
authors = [{ name = "BJS", email = "coderbjs@gmail.com" }]
requires-python = ">=3.13.7"
license = { text = "MIT" }

dependencies = ["coloram==0.4.6", "numpy== 2.3.5"]
[tool.poetry]

package-mode = true

[tool.poetry.dependencies]

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonary.api"
