[project]
name = "xiaobo-task"
version = "1.0.6"
description = "自用多线程任务模块"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "curl-cffi>=0.13.0",
    "loguru>=0.7.3",
    "pydantic-settings>=2.12.0",
    "python-dotenv>=1.2.1",
    "tenacity>=9.1.2",
]
authors = [
  { name = "xiaobo", email = "tb791549729@gmail.com" }
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"