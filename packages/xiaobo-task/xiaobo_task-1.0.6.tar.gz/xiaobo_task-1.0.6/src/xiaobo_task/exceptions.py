# -*- coding: utf-8 -*-

"""
自定义异常类模块

该模块定义了框架特有的异常类型，用于控制任务执行流程。
"""


class TaskFailed(Exception):
    """
    任务失败异常

    当任务抛出此异常时，框架将不会进行重试，直接标记任务为失败。

    使用场景:
        - 当任务遇到不可恢复的错误时
        - 当重试没有意义时（如参数错误、权限不足等）
        - 当需要立即终止任务并记录失败时
    """
    pass
