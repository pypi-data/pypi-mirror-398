[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "modelsandboxprotocol"
version = "0.0.0"
authors = [{name = ""}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.7"
