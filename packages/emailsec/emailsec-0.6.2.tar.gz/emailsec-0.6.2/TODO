 - DMARC should return temp/perm error status
 - see if we actually need to return the whole signature for DKIM
 - see if the "too many DNS resolutions" issue is handled + unit test
 - make a "Projects" page on hexa.ninja (Active + Dead sections)
