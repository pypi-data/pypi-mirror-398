[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyjquants"
version = "0.1.0"
description = "Investor-friendly OOP Python library for J-Quants API"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Soshi Mizutani" }
]
keywords = ["jquants", "stock", "trading", "finance", "japan", "api"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Financial and Insurance Industry",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business :: Financial :: Investment",
    "Typing :: Typed",
]
dependencies = [
    "requests>=2.31",
    "pandas>=2.0",
    "pydantic>=2.0",
    "python-dateutil>=2.8",
]

[project.optional-dependencies]
async = ["aiohttp>=3.9"]
cache = ["diskcache>=5.6"]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "pytest-cov>=4.0",
    "mypy>=1.0",
    "ruff>=0.1",
    "types-requests",
    "pandas-stubs",
]
all = [
    "aiohttp>=3.9",
    "diskcache>=5.6",
]

[project.urls]
Homepage = "https://github.com/obichan117/pyjquants"
Documentation = "https://github.com/obichan117/pyjquants#readme"
Repository = "https://github.com/obichan117/pyjquants.git"

[tool.hatch.build.targets.wheel]
packages = ["pyjquants"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # Pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "UP",     # pyupgrade
]
ignore = [
    "E501",   # line too long (handled by formatter)
]

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_ignores = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["pyjquants"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
]
