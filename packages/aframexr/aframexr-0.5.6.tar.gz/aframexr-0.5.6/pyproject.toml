[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aframexr"
version = "0.5.6"
authors = [{name = "David Díaz"}]
description = "Python library to visualize data in Virtual and Extended Reality using Aframe components."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.20.0",  # For Pyodide compatibility
    "marimo>=0.18.0",
]

[project.urls]
Documentation = "https://davidlab20.github.io/TFG/"
Source = "https://github.com/davidlab20/TFG/"

[project.optional-dependencies]
pandas = [
    "pandas>=2.3.0",
]
dev = [
    "beautifulsoup4>=4.14.0",
    "pytest>=9.0.0",
]
