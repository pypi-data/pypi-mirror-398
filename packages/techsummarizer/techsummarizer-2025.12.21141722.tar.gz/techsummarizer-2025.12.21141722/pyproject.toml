[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "techsummarizer"
version = "2025.12.21141722"
description = "A new package is designed to analyze and summarize technical articles or announcements by extracting structured key information from user-provided text. It leverages language models to identify and or"
readme = "README.md"
authors = [{ name = "techsummarizer", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/techsummarizer"
