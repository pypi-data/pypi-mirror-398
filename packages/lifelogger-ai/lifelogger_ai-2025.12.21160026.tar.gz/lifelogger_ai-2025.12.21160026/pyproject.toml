[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lifelogger-ai"
version = "2025.12.21160026"
description = "A new package that helps users capture and organize their life moments in a structured way. Users can input their experiences, thoughts, or significant events, and the package will process this text t"
readme = "README.md"
authors = [{ name = "lifelogger-ai", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/lifelogger-ai"
