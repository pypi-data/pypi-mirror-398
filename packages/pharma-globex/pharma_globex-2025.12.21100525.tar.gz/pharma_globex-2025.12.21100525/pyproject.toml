[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pharma-globex"
version = "2025.12.21100525"
description = "A new package designed to analyze input texts related to global pharmaceutical trends, such as India's role in manufacturing generics, and generate structured summaries or insights. It leverages struc"
readme = "README.md"
authors = [{ name = "pharma-globex", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/pharma-globex"
