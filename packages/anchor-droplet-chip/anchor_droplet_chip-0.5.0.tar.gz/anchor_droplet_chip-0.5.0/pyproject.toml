[build-system]
requires = [
  "setuptools >= 40.9.0",
  "wheel",
  "setuptools-scm",

]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "src/adc/_version.py"

[tool.black]
line-length = 79

[tool.isort]
profile = "black"
line_length = 79
