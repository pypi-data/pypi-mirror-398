[project]
name = "httpxx"
version = "0.1.2"
description = "httpx的封装"
keywords = ["http", "cache", "httpx"]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "httpx>=0.28.1",
]
license = {text = "MIT"}

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
