[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "texttoknowledge"
version = "2025.12.21231444"
description = "Convert unstructured text into structured, queryable knowledge with llmatch-messages. Extract and organize key details for fast, reliable access—ideal for teams and researchers."
readme = "README.md"
authors = [{ name = "texttoknowledge", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/texttoknowledge"
