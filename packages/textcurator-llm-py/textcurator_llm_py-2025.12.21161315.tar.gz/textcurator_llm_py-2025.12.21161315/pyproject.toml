[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "textcurator-llm-py"
version = "2025.12.21161315"
description = "A new package would process user-provided text inputs, such as headlines or short descriptions, and generate structured summaries or categorizations using an LLM. It would be particularly useful for c"
readme = "README.md"
authors = [{ name = "textcurator-llm-py", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/textcurator-llm-py"
