[pytest]
markers =
    unit: fast, isolated tests
    e2e: end-to-end tests that may hit filesystem, subprocesses, etc.
    tool_required(name): tests that require an external tool binary to be present in PATH
testpaths = ../../../../test/curvtools/memmap2
