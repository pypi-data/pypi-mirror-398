[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sast-secureai"
version = "2025.12.21084550"
description = "Automates Software Security Analysis and Testing (SAST) by receiving user-inputted code snippets or text descriptions of functionality and returning a structured response highlighting potential securi"
readme = "README.md"
authors = [{ name = "sast-secureai", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/sast-secureai"
