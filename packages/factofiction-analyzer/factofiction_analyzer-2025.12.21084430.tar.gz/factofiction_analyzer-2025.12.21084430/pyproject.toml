[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "factofiction-analyzer"
version = "2025.12.21084430"
description = "A new package designed to help users distinguish between factual information and imaginative interpretations. This tool takes user-provided text as input and uses advanced language models to analyze a"
readme = "README.md"
authors = [{ name = "factofiction-analyzer", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/factofiction-analyzer"
