
from pydantic import BaseModel, Field
from AgentService.agent.tool import AgentTool


class ${tool_name}Parameters(BaseModel):
    location: str = Field(..., description="Location to get weather")


class ${tool_name}Tool(AgentTool):
    name = "get_weather"
    description = "Function to get weather"
    parameters = ${tool_name}Parameters
