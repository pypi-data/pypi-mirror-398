[project]
name = "drf-discord-handler"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
authors = [
    { name="shinkeonkim", email="dev.shinkeonkim@gmail.com" }
]
license= { text = "MIT" }
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "django>=6.0",
    "djangorestframework>=3.16.1",
]

[project.urls]
Homepage = "https://github.com/shinkeonkim/drf_discord_handler"
Repository = "https://github.com/shinkeonkim/drf_discord_handler"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = { find = {} }

[tool.setuptools.package-data]
"*" = ["py.typed"]
