/**
 * Includes GL headers.
 */
#ifndef __gl_redirect_h_
/* backup file removed: leave a benign marker so packaging won't pull desktop
   GL headers from this path during pip builds. */

#ifndef __gl_redirect_h_backup_marker_
#define __gl_redirect_h_backup_marker_

/* intentionally empty - backup disabled */

#endif
#ifdef ANDROID
