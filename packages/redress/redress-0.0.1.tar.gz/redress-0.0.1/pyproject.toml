[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "redress"
version = "0.0.1"
description = "Placeholder for upcoming resilience and retry library."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "aponysus", email = "aponysus@users.noreply.github.com" },
]
classifiers = [
  "Development Status :: 1 - Planning",
]

[tool.hatch.build.targets.wheel]
packages = []  # Empty package for reservation