[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gameconcept-analyzer"
version = "2025.12.21143849"
description = "A new package that processes user-submitted text descriptions of local multiplayer party game concepts and returns structured feedback or suggestions. It helps game developers or enthusiasts by analyz"
readme = "README.md"
authors = [{ name = "gameconcept-analyzer", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/gameconcept-analyzer"
