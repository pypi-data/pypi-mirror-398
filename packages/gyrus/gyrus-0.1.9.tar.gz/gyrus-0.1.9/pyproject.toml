[project]
name = "gyrus"
version = "0.1.9"
description = "A lightweight workflow engine with YAML-based configuration."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
