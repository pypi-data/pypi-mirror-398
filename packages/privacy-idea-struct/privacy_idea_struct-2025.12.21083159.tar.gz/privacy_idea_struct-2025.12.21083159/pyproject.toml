[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "privacy-idea-struct"
version = "2025.12.21083159"
description = "A new package that enables users to provide simple text inputs about innovative, privacy-focused services—such as a phone company that doesn't collect personal data—and receive structured summaries or"
readme = "README.md"
authors = [{ name = "privacy-idea-struct", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/privacy-idea-struct"
