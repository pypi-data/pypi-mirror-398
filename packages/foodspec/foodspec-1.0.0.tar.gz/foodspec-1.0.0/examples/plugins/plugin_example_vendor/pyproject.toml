[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "plugin_example_vendor"
version = "0.0.1"
dependencies = ["foodspec"]

[project.entry-points."foodspec.plugins"]
plugin_example_vendor = "plugin_example_vendor:plugin_main"
