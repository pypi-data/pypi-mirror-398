# Plugin Example – Protocol

Minimal FoodSpec plugin that registers a custom protocol template.

Install for testing:
```bash
pip install -e .
```

Then:
```bash
foodspec-plugin list
```
You should see `Example_Plugin_Protocol`.

The protocol can be loaded via the plugin manager or directly by name.
