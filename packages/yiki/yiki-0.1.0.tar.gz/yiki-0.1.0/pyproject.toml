[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yiki"
version = "0.1.0"
description = "yiki my own tool kit"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "yiki", email = "admin@yiki.io" }]
requires-python = ">=3.8"