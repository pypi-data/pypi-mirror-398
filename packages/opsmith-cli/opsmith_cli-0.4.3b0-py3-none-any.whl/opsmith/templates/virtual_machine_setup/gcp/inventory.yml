all:
  hosts:
    gce-instance:
      ansible_host: "{{ private_ip }}"
      ansible_user: "{{ user }}"
      ansible_ssh_common_args: >
        -o ProxyCommand="gcloud compute start-iap-tunnel {{ instance_id}} 22 --listen-on-stdin
        --project {{ project_id }} --zone {{ zone }}"
