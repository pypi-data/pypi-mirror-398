[pytest]
minversion = 8.0
addopts = -ra
testpaths =
    tests
pythonpath =
    src
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: marks tests as integration (deselect with '-m "not integration"')
