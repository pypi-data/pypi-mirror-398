[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "qabbage"
version = "0.0.1"
description = "Qabbage client SDK (thin client)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/qabbage"]
