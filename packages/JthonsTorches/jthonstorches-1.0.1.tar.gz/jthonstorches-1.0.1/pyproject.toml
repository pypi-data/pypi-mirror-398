[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "JthonsTorches"
version = "1.0.1"
authors = [
    { name= "APomson6000UserWhoEats6000IrisFlowers" }
]
description = "I'd reckon that this library is pretty useful, right? Like, it's a Java IDE in Python! Who wouldn't want that?"
license = { file= "License" }