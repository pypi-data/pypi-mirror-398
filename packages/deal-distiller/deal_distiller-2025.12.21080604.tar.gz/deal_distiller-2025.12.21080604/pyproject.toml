[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "deal-distiller"
version = "2025.12.21080604"
description = "This package aims to distill complex business and technology news into easily digestible summaries, focusing on the core implications and potential impact of mergers and acquisitions. It tackles the p"
readme = "README.md"
authors = [{ name = "deal-distiller", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/deal-distiller"
