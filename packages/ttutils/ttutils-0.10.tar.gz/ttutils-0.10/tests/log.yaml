version: 1
disable_existing_loggers: False


handlers:
  console:
    class: logging.StreamHandler
    stream: ext://sys.stdout
    level: DEBUG


root:
  handlers:
    - console
  propagate: False
  level: DEBUG
