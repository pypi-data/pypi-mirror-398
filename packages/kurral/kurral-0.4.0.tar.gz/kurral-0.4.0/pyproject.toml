[build-system]
requires = ["setuptools>=61.0.0,<68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kurral"
version = "0.4.0"
description = "Deterministic testing and replay framework for AI agents"
readme = "README.md"
requires-python = ">=3.9"
authors = [
    {name = "Kurral Team", email = "team@kurral.com"}
]
keywords = [
    "llm",
    "testing",
    "replay",
    "determinism",
    "agents",
    "langchain",
    "ai-agents",
    "regression-testing",
    "ab-testing",
    "mcp",
    "model-context-protocol",
    "observability",
    "sse",
    "streaming"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Testing",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "pydantic>=2.5.0",
    "click>=8.1.7",
    "rich>=13.7.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
langchain = [
    "langchain>=0.1.0",
    "langchain-openai>=0.0.5",
]
openai = [
    "openai>=1.0.0",
]
anthropic = [
    "anthropic>=0.18.0",
]
groq = [
    "groq>=0.4.0",
]
google = [
    "google-generativeai>=0.3.0",
]
mcp = [
    "fastapi>=0.100.0",
    "uvicorn>=0.23.0",
    "httpx>=0.24.0",
    "sse-starlette>=1.6.0",
]
all = [
    "kurral[langchain,openai,anthropic,groq,google,mcp]",
]
dev = [
    "pytest>=7.4.3",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.1.0",
    "black>=23.12.0",
    "ruff>=0.1.9",
    "mypy>=1.8.0",
    "build>=1.0.0",
    "twine>=4.0.0",
]

[project.urls]
Homepage = "https://github.com/Kurral/Kurralv3"
Documentation = "https://github.com/Kurral/Kurralv3#readme"
Repository = "https://github.com/Kurral/Kurralv3"
Issues = "https://github.com/Kurral/Kurralv3/issues"
Discord = "https://discord.gg/pan6GRRV"

[project.scripts]
kurral = "kurral.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["kurral*"]
exclude = ["tests*", "examples*"]

[tool.setuptools.package-data]
kurral = [
    "py.typed",
    "quickstart/templates/**/*.template",
    "quickstart/templates/**/*.yml",
    "quickstart/templates/**/*.yaml",
]

[tool.black]
line-length = 100
target-version = ['py39', 'py310', 'py311', 'py312']

[tool.ruff]
line-length = 100
target-version = "py39"
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
]
ignore = [
    "E501",  # line too long (handled by black)
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --cov=kurral --cov-report=term-missing"
asyncio_mode = "auto"

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false

[tool.coverage.run]
source = ["kurral"]
omit = [
    "tests/*",
    "*/tests/*",
    "*/__pycache__/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]