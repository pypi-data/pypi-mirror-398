[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-claim-essence"
version = "2025.12.20202921"
description = "A new package that lets users input a short statement about AI’s societal impact and instantly receives a clear, structured summary of the core idea. By focusing on extracting the essence of the claim"
readme = "README.md"
authors = [{ name = "ai-claim-essence", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/ai-claim-essence"
