[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bitcorex"
version = "0.1.0"
description = "Bitwise operations library for Python"
readme = "README.md"
authors = [{ name="Odiljonov Boburmirzo", email="odiljonovboburmirzo17@gmail.com" }]
requires-python = ">=3.7"