[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "autopyproject"
version = "0.1.1"
description = "Python project scaffolder"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Ismail Farooq" }]
dependencies = []

[project.scripts]
createproject = "autopyproject.autopyproject:main"

[tool.setuptools]
package-dir = {"" = "src"}
