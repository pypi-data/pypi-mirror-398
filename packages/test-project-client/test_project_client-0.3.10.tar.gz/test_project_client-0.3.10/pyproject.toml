[tool.poetry]
name = "test-project-client"
version = "0.3.10"
description = "Auto-generated connector service - Client Library"
authors = ["Holland Generator"]
packages = [
    { include = "src", from = ".", to = "test_project_client" },
    { include = "openapi", from = ".", to = "test_project_client/openapi" }
]

[tool.poetry.dependencies]
python = "^3.9"
pydantic = "^2.4.0"
httpx = "^0.25.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0.0"
pytest-asyncio = "^0.21.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
