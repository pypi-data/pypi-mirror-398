[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gxd"
version = "0.0.0a2"
description = "GXD – Block-based compression, algorithm analysis, and integrity tooling"
readme = "README.md"
license = { text = "GPL-3.0" }
authors = [
  { name = "Muhammed Shafin p", email = "not-required@example.com" }
]
requires-python = ">=3.8"

dependencies = [
  "tqdm; extra == 'progress'"
]

[project.optional-dependencies]
full = ["zstandard", "lz4", "brotli", "tqdm"]

[project.urls]
Homepage = "https://github.com/hejhdiss/GXD"
Source = "https://github.com/hejhdiss/GXD"

[project.scripts]
gxd = "gxd.cli:main"
gxd-algo = "gxd.algo_cli:main"
gxd-sign = "gxd.signer_cli:main"
