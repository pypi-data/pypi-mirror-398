[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "phtokenizer"
version = "1.0.0"
description = "Dynamic hybrid tokenizer for micro-datasets"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.7"

[tool.setuptools.packages.find]
where = ["."]