[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "uaal-core"
version = "0.1.3"
description = "UAAL core decision ledger library"
requires-python = ">=3.9"
dependencies = []

[project.urls]
repository = "https://github.com/LOLA0786/UAAL"
