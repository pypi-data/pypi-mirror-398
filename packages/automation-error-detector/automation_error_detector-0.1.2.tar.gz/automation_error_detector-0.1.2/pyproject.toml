[project]
name = "automation-error-detector"
version = "0.1.2"
description = "Automation browser error detection with AI fallback and cache"
authors = [{ name="Your Name" }]
readme = "README.md"
requires-python = ">=3.9"

dependencies = [
  "openai>=1.0.0"
]

[tool.setuptools.packages.find]
where = ["."]
