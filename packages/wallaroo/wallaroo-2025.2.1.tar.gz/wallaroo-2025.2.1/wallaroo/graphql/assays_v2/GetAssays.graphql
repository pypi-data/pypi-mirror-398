query GetAssays($_in: [Boolean!]!) {
  assays_v2(where: {active: {_in: $_in}}, order_by: {created_at: desc}) {
    baseline
    active
    id
    summarizer
    targeting
    window
    scheduling
    created_at
    updated_at
    name
  }
}
