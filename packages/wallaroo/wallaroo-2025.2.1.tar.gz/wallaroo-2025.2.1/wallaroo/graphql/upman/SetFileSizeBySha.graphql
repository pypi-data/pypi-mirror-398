mutation SetFileSizeBySha($sha: bpchar!, $size: bigint!) {
  update_model(where: {sha: { _eq: $sha } }, _set: { file_size: $size }) {
    returning { id }
  }
  update_orchestration(where: {sha: { _eq: $sha } }, _set: { file_size: $size }) {
    returning { id }
  }
}