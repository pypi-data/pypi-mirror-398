mutation SetTaskKilled(
    $id: uuid!,
    $status: task_status_enum!,
) {
    update_task_by_pk(
        pk_columns: { id: $id }
        _set: { killed: true, status: $status }
    ) {
        id
        killed
    }
}
