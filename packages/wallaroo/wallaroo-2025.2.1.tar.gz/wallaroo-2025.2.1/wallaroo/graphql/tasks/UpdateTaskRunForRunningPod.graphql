mutation UpdateTaskRunForRunningPod($pod_id: uuid!, $status: String!) {
  update_task_run(where: { _and: { pod_id: { _eq: $pod_id}, status: { _eq: "running" } } } , _set: {status: $status}) {
    affected_rows
  }
}
