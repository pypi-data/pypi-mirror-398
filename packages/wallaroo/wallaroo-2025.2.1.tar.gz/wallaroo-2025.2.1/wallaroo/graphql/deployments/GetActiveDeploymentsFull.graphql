query GetActiveDeploymentsFull {
  deployment(where: {deployed: {_eq: true}}) {
    id
    deploy_id
    engine_config
    deployed
    node_selector
    created_at
    updated_at
    updated_by
  }
}
