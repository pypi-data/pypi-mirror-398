query GetConnection($name: String!) {
    connections(where: {name: {_eq: $name}}) {
        id
        name
        type
        details
        created_at
        workspace_connections {
            workspace {
                name
            }
        }
    }
}
