query GetConnectionById($id: uuid!) {
    connections_by_pk(id: $id) {
        id
        name
        type
        details
        created_at
        workspace_connections {
            workspace {
                name
            }
        }
    }
}
