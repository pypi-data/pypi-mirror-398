query GetModel($id: bigint!) {
  model(where: {id: { _eq: $id}}) {
      sha
      models_pk_id
      model_version
      owner_id
      model_id
      id
      file_name
      image_path
      visibility
      status
      task_id
      conversion
      model {
        workspace_id
      }
    }
}
