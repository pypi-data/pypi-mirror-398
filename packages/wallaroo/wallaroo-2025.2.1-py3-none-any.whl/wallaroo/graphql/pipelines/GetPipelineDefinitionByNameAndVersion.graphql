query GetPipelineDefinitionByNameAndVersion($version: uuid!) {
  pipeline_version(limit: 1, where: {version: {_eq: $version}}) {
    definition
    version
    pipeline {
      pipeline_id
    }
  }
}