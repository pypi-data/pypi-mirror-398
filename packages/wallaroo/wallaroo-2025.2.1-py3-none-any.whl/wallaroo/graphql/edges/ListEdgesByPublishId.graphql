query ListEdgesByPublishId($publish_id: bigint!) {
  edge(where: {edge_should_run_publish: {id: {_eq: $publish_id}}}) {
    tags
    spiffe_id
    name
    memory
    id
    created
    cpus
    should_run_publish
    created_on_version
  }
}
