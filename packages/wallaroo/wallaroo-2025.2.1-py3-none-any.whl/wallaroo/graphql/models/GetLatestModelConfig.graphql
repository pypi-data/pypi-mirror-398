query GetLatestModelConfig($model_id: bigint!) {
	model_config(where: {
    model_id: {
      _eq:$model_id
    }
  }, limit:1, order_by: {id: desc}) {
    id
    runtime
		tensor_fields
    filter_threshold
    input_schema
    output_schema
  }
}