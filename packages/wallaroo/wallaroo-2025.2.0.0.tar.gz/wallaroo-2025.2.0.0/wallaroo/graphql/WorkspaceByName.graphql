query WorkspaceByName($name: string!) {
    workspace(where: {name: {_eq: $name}}) {
        archived
        created_at
        created_by
        id
        name
        users {
            user_id
        }
        models {
            models {
            id
            }
        }
        pipelines {
            id
        }
    }
}
