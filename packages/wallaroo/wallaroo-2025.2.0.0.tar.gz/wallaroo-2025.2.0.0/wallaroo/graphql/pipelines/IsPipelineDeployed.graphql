query IsPipelineDeployed($pipeline_id: String!, $workspace_id: bigint!) {
  pipeline(where: {_and: {
    pipeline_id: {_eq: $pipeline_id},
    workspace_id: {_eq: $workspace_id}
  }}) {
    deployment {
      deployed
    }
  }
}
