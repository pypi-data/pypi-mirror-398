query GetAllPipelineVersions ($versions: [uuid!]!) {
  pipeline(where: {pipeline_versions: {version: {_in: $versions} } }) {
		pipeline_id
    workspace_id
    pipeline_versions {
      version
    }
  }
}
