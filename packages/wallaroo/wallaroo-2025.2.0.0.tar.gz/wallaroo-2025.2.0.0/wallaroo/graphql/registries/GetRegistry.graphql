query GetRegistry($id: uuid!) {
  model_registries_by_pk(id: $id) {
    created_at
    id
    name
    token
    updated_at
    url
    workspace_model_registries {
      workspace {
        id
        name
      }
    }
  }
}
