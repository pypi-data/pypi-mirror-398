mutation StartTaskRun(
    $id:uuid!,
    $attempt:Int!,
    $status: task_status_enum!
) {
    insert_task_output_one(object: {
        id:$id,
        attempt: $attempt,
        status: $status
    }) {
        id
        status
        attempt
        status
    }
}
