query WorkspaceListConnections($id: bigint!) {
  workspace_connections(where: {workspace_id: {_eq: $id}}) {
    connection {
      id
      name
      type
      details
      created_at
      workspace_connections {
        workspace {
          name
        }
      }
    }
  }
}
