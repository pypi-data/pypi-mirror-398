query GetAssaysForPipeline($pipeline_id: String!, $workspace_id: bigint!) {
  assays_v2(where: {
    pipeline: {
      _and: {
        workspace_id: {_eq: $workspace_id},
        pipeline_id: {_eq: $pipeline_id}
      }
    }
  }) {
    id
  }
}
