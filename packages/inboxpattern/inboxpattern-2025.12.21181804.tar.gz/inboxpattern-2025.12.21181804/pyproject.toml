[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inboxpattern"
version = "2025.12.21181804"
description = "A new package that helps users and organizations analyze and categorize email account usage patterns. The package takes user-submitted text input describing their email management habits and returns a"
readme = "README.md"
authors = [{ name = "inboxpattern", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/inboxpattern"
