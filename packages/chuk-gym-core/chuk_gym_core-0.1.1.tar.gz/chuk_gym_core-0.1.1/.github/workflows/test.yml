name: Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  workflow_call:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.11", "3.12", "3.13"]

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        enable-cache: true

    - name: Set up Python ${{ matrix.python-version }}
      run: uv python install ${{ matrix.python-version }}

    - name: Install dependencies
      run: uv sync --frozen --extra dev

    - name: Run linting
      run: |
        uv run ruff check --fix .
        uv run ruff format .
        git diff --exit-code || (echo "Linting made changes. Please run 'uv run ruff check --fix . && uv run ruff format .' locally and commit." && exit 1)

    - name: Run type checking
      run: |
        uv run python -c "import chuk_gym_core.env.base; print('Import works')"
        uv run mypy --package chuk_gym_core

    - name: Run tests with coverage
      run: |
        uv run pytest tests/ --cov=src/chuk_gym_core --cov-report=term --cov-report=xml --cov-report=html

    - name: Upload coverage reports
      if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

    - name: Check coverage threshold
      if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
      run: |
        uv run python -c "
        import xml.etree.ElementTree as ET
        tree = ET.parse('coverage.xml')
        root = tree.getroot()
        coverage = float(root.attrib['line-rate']) * 100
        print(f'Coverage: {coverage:.2f}%')
        if coverage < 90:
            print(f'Coverage {coverage:.2f}% is below 90% threshold')
            exit(1)
        "
