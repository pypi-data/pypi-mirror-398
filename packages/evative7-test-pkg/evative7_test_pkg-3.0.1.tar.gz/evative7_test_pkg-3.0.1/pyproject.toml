[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
authors = [{ name = "EvATive7" }]
classifiers = [ # TODO: Finish this
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
]
dependencies = [] # TODO: Finish this
description = "" # TODO: Finish this
name = "evative7_test_pkg" # TODO: Finish this
readme = "README.md"
requires-python = ">=3.10" # TODO: Finish this
dynamic = ['version']
# license = { text = "MIT" } # TODO: Finish this

[project.optional-dependencies]
dev = ["ruff", "pytest", "hatchling", "hatch-vcs", "editables"] # TODO: Finish this

# [project.scripts]
# helloworld = "hello_world:main" # TODO: Finish this, if a script is needed

[project.urls]
"Homepage" = "https://github.com/EvATive7/hello_world" # TODO: Finish this

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["test"]

[tool.hatch.build.targets.sdist]
exclude = [
  "/.github",
  "/.vscode",
  "/uv.lock",
  "/.gitignore",
]

[tool.hatch.version]
source = "vcs"

# TODO: Finish this file if you need to add anything
