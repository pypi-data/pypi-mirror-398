[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "django-system-audit"
version = "0.1.1"
description = "Explicit, intent-aware audit trail library for Django and Django REST Framework"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
    {name = "Rajat Jog", email = "rajatjog1294@gmail.com"}
]
dependencies = [
    "Django>=3.2"
]
[project.urls]
"Homepage" = "https://github.com/RJ-Gamer/django-activity-tracker"