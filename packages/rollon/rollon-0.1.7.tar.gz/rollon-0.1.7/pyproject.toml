[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rollon"
version = "0.1.7"
description = "Roll-On: Descargas con yt-dlp, UI limpia y portada Apple para OPUS (menú interactivo)."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{ name = "Cesarx9" }]
dependencies = [
  "yt-dlp>=2025.1.0 ; python_version >= '3.8'"
]

[project.scripts]
rollon = "rollon.menu:main"

[tool.setuptools]
packages = ["rollon"]
