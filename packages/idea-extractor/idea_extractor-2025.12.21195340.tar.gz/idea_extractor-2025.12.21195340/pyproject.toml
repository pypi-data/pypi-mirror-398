[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "idea-extractor"
version = "2025.12.21195340"
description = "idea-extractor extracts, categorizes, and scores high‑impact software ideas from forum discussions, outputting themes, feasibility and market impact."
readme = "README.md"
authors = [{ name = "idea-extractor", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/idea-extractor"
