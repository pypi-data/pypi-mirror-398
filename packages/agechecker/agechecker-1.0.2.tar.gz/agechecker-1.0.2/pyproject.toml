[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agechecker"
version = "1.0.2"
dependencies = [] # Add requirements here if you have any
description = "A simple age checking utility"
readme = "README.md"
authors = [{name = "Your Name"}]

[tool.setuptools]
packages = ["agechecker"]