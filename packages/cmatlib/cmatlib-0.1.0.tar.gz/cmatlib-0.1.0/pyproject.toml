[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cmatlib"
version = "0.1.0"
description = "Синхронная библиотека для работы с ChatGPT с историей сессии"
authors = [{name = "Toqrul"}]
dependencies = ["openai>=1.0.0"]
readme = "README.md"
