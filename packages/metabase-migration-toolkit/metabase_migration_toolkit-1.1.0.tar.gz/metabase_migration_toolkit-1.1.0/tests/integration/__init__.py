"""
Integration tests for Metabase Migration Toolkit.

These tests require a running Metabase instance and are marked with
the @pytest.mark.integration decorator.

To run integration tests:
    pytest -m integration

To skip integration tests:
    pytest -m "not integration"
"""
