[project]
name = "satori-client"
version = "0.1.8"
description = "Python client for Satori database"
authors = [{name="Sergio Valdes", email="svcu2310@gmail.com"}]
readme = "README.md"
requires-python = ">=3.7"
dependencies = ["websockets", "uuid"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.hatch.build.targets.wheel]
packages = ["satori"]