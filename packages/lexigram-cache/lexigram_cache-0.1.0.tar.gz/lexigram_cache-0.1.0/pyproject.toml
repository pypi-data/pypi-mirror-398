[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lexigram-cache"
version = "0.1.0"
description = "Reserved package for lexigram-cache"
authors = [
    {name = "Lexigram Framework Team", email = "team@lexigram-framework.dev"}
]
requires-python = ">=3.11"
readme = "README.md"

[tool.setuptools.packages.find]
where = ["src"]
