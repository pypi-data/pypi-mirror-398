[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "algopract"
version = "0.1.0"
description = "A learning-focused Python package implementing core algorithms with step-by-step explanations."
authors = [
    { name = "Loka Ram Kalyan Are", email = "alrk6125@gmail.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
