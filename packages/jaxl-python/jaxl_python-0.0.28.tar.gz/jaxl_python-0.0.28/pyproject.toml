[build-system]
requires = [
  # Essentials
  "setuptools",
  "wheel",

  # Plugins
  "setuptools-scm[toml]==8.3.1",
  "setuptools-scm-git-archive >= 1.1",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "jaxl/api/client/_scm_version.py"
