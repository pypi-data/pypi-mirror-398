[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "access-specifiers"
version = "1.5.0"
dependencies = ["dill"]
description = "Fully featured access modifier library for python"
readme = "README.md"
authors = [{name = "pyhacks", email = "enginyildirim111@gmail.com"}]
keywords = ["access", "modifier", "specifier"]

[project.urls]
Homepage = "https://github.com/pyhacks/access-specifiers"