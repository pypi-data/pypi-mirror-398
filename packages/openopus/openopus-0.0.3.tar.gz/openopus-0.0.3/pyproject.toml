[project]
name = "openopus"
version = "0.0.3"
dependencies = ["requests"]
requires-python = ">=3.10"
authors = [
    { name = "Joonbo Shim", email = "joonbo@gmail.com" }
]
description = "Python client and service layer for Open Opus API with caching and normalized models"
readme = "README.md"
license = "MIT"
license-files = ["LICEN[CS]E*"]
keywords = ["Open Opus", "classical music", "classical"]
classifiers = [
    "Programming Language :: Python"
]
