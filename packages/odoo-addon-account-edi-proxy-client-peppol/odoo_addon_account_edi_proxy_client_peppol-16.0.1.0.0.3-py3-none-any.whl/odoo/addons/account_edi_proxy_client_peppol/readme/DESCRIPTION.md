This module adds generic features to register an Odoo DB on the proxy
responsible for exchaning peppol data.

This is a backport of the account_edi_proxy_client module from Odoo 17,
with a different model name to avoid conflicts with the original module
in 16.
