[project]
name = "yccdb"
version = "0.5.0"
description = "a db api"
readme = "README.md"                        # ✅ makes your PyPI page show the README
requires-python = ">=3.0"
dependencies = ["requests"]
authors = [
    { name="Th3ou1d3x", email="Th3ou1d3x@bloodcircuit.org"},
    { name="Anthony", email="Anthony@bloodcircuit.org" }
]
license = { text = "MIT" }                  # optional but recommended
classifiers = [                             # helps PyPI categorize it
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent"
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"