[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "learnify"
version = "2025.12.21123742"
description = "A new package that helps software developers improve their learning strategies by analyzing their self-reported learning habits, goals, and challenges. It takes text input describing their current lea"
readme = "README.md"
authors = [{ name = "learnify", email = "hi@eugene.plus" }]
requires-python = ">=3.9"
dependencies = [
  "langchain-llm7>=0.0.0",
  "llmatch-messages>=0.0.0",
  "langchain-core>=0.3.0",
]
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/chigwell/learnify"
