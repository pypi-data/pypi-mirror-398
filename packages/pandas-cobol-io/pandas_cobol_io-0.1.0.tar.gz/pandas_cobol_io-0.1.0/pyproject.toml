[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pandas-cobol-io"
version = "0.1.0"
description = "Pandas extension for COBOL-style fixed-width files handling multibyte encoding."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
  { name="jianwu", email="yi1meir4@gmail.com" },
]
dependencies = [
    "pandas",
]
