[project]
name = "gpu_benchmark"
version = "0.4.2"
description = "GPU benchmarking tool for training and inference"
readme = "README.md"
authors = [
    {name = "Roland Graser", email = "roland@unitedcompute.ai"},
    {name = "Max Hager", email = "maxhager28@gmail.com"}
]
dependencies = [
    "torch",
    "tqdm",
    "diffusers",
    "transformers",
    "accelerate",
    "pynvml",
    "supabase",
    "tiktoken",
    "requests"
]

[project.scripts]
gpu-benchmark = "gpu_benchmark.main:main"