[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hbin"
version = "0.1.0"
description = "Save Bash history as snippets / history tooling"
readme = "README.md"
requires-python = ">=3.9"
authors = [
    { name = "LM Web Tech." }
]

dependencies = []

[project.scripts]
hbin = "hbin.cli:main"