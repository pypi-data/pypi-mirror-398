[build-system]
requires = ["setuptools >= 68.2.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sceala"
version = "0.0.0"
authors = [
  { name="Riain Ó Tuathail", email="rotuathail01@gmail.com" },
]
description = "Complement for nuacht which pulls stories from the entries in the database."
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]
dependencies = [
    "beautifulsoup4==4.14.3",
    "requests==2.32.5",
    "tldextract==5.3.0"
]