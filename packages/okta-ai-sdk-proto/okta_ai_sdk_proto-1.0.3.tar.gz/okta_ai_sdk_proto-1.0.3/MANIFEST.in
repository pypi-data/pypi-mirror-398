# Include LICENSE file (required for PyPI, not automatically included)
include LICENSE

# Include examples (optional but useful for users)
recursive-include examples *.py
recursive-include examples *.md

# Exclude test folder (contains local test files with credentials)
prune test

