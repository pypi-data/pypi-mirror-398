[project]
name = "tuple-list-benchmark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pyperf",
    "tyro>=1.0.2",
]

[project.scripts]
tuple-list-benchmark = "tuple_list_benchmark.bench:main"

[tool.uv]
package = true
