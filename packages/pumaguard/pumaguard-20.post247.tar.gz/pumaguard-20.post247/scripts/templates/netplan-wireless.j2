network:
  version: 2
  renderer: NetworkManager
  wifis:
    wlan0:
      optional: true
      dhcp4: true
      access-points:
        {% for network in wireless_networks %}
        "{{ network.ssid }}":
          {% if network.password %}
          password: "{{ network.password }}"
          {% else %}
          {}
          {% endif %}
        {% endfor %}
